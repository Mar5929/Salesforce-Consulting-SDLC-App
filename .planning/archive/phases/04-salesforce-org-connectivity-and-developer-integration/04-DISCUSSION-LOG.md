# Phase 4: Salesforce Org Connectivity and Developer Integration - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-06
**Phase:** 04-salesforce-org-connectivity-and-developer-integration
**Areas discussed:** Org Connection Flow, Metadata Sync Strategy, Brownfield Ingestion UX, REST API for Claude Code

---

## Org Connection Flow

| Option | Description | Selected |
|--------|-------------|----------|
| OAuth 2.0 Web Server Flow | Standard authorization code grant with redirect | |
| Claude's recommendation (all) | One org/project, OAuth 2.0, SA/PM only, encrypted tokens, auto-refresh | |

**User's choice:** Accepted Claude's recommended defaults for all decisions (D-01 through D-05)
**Notes:** User requested Claude select recommended options for all areas. OAuth 2.0 Web Server Flow, one org per project, SA/PM role restriction, encrypted tokens with auto-refresh and failure notification.

---

## Metadata Sync Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Claude's recommendation (all) | Core consulting metadata types, daily incremental + on-demand full, Inngest cron | |

**User's choice:** Accepted Claude's recommended defaults for all decisions (D-06 through D-09)
**Notes:** Sync core consulting-relevant metadata types. Two sync modes (full + incremental). Daily Inngest cron with configurable frequency. Status card with sync history.

---

## Brownfield Ingestion UX

| Option | Description | Selected |
|--------|-------------|----------|
| Interactive chat session | Like transcript processing — real-time conversation with AI during ingestion | |
| Background job with status page | Inngest step function, bulk processing, architect reviews after completion | |

**User's choice:** Accepted Claude's recommendation: Background Inngest job (D-10 through D-14)
**Notes:** Bulk analysis not suited for real-time chat. Architect review via accept/reject cards (reusing Phase 2 pattern). Manual trigger by SA. Tabbed review view for domain groupings and business processes.

---

## REST API for Claude Code

| Option | Description | Selected |
|--------|-------------|----------|
| Project-scoped API keys | Generated by SA/PM, hashed in DB, shown once | |
| Clerk token forwarding | Reuse existing auth, but complex for CLI tools | |
| MCP server | Model Context Protocol server for Claude Code | |
| Direct REST calls | Skills make HTTP calls with API key | |

**User's choice:** Accepted Claude's recommendation: Project-scoped API keys + direct REST (D-15 through D-19)
**Notes:** API keys simpler than Clerk for CLI. Versioned `/api/v1/` namespace. Context package reuses Phase 2 assembly. No MCP in V1.

---

## Claude's Discretion

- API rate limiting strategy and limits
- Exact metadata type list fine-tuning
- Sync job retry and error handling
- Ingestion pipeline AI prompts and token budgets
- API response pagination
- Org analysis progress indicator design
- Loading states and error boundaries

## Deferred Ideas

None — discussion stayed within phase scope.
