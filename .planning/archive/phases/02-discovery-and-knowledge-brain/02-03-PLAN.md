---
phase: 02-discovery-and-knowledge-brain
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/api/chat/route.ts
  - src/actions/conversations.ts
  - src/components/chat/chat-interface.tsx
  - src/components/chat/message-list.tsx
  - src/components/chat/message-bubble.tsx
  - src/components/chat/context-panel.tsx
  - src/components/chat/token-display.tsx
  - src/app/(dashboard)/projects/[projectId]/chat/page.tsx
  - src/app/(dashboard)/projects/[projectId]/chat/[conversationId]/page.tsx
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05]

must_haves:
  truths:
    - "User can open general project chat from sidebar and send messages with streaming AI responses"
    - "User can open task-specific chat sessions with split layout showing context panel"
    - "All conversations persist with full message history across page refreshes"
    - "Token usage and cost display as subtle footer metadata per AI message"
    - "Session total token count and cost shown in chat header"
  artifacts:
    - path: "src/app/api/chat/route.ts"
      provides: "Vercel AI SDK streaming endpoint connected to Claude"
      exports: ["POST"]
    - path: "src/components/chat/chat-interface.tsx"
      provides: "Main chat component with useChat hook"
      exports: ["ChatInterface"]
    - path: "src/components/chat/message-list.tsx"
      provides: "Scrollable message list with date groupings"
      exports: ["MessageList"]
    - path: "src/components/chat/context-panel.tsx"
      provides: "Right panel for task sessions showing entities and token usage"
      exports: ["ContextPanel"]
    - path: "src/actions/conversations.ts"
      provides: "Server actions for conversation CRUD"
      exports: ["createConversation", "getConversation", "getConversations", "getOrCreateGeneralChat"]
  key_links:
    - from: "src/components/chat/chat-interface.tsx"
      to: "src/app/api/chat/route.ts"
      via: "useChat hook with api='/api/chat'"
      pattern: "useChat.*api.*chat"
    - from: "src/app/api/chat/route.ts"
      to: "@ai-sdk/anthropic"
      via: "streamText with anthropic provider"
      pattern: "streamText.*anthropic"
    - from: "src/app/api/chat/route.ts"
      to: "src/lib/agent-harness/context/"
      via: "context assembly for system prompt"
      pattern: "assembleGeneralChatContext|getProjectSummary"
---

<objective>
Chat infrastructure: streaming API route, conversation persistence, and chat UI components

Purpose: Chat is the primary interaction surface for the AI brain. General project chat lets users ask ad-hoc questions and get AI responses grounded in project context. Task-specific sessions (for transcript processing, briefings) need a split layout with context metadata. This plan delivers the chat plumbing that transcript processing (Plan 05) and other features build on.

Output: Working streaming chat with persistent message history, general chat (full-width) and task session (split layout) modes, token/cost tracking per message and session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-discovery-and-knowledge-brain/02-CONTEXT.md
@.planning/phases/02-discovery-and-knowledge-brain/02-RESEARCH.md
@.planning/phases/02-discovery-and-knowledge-brain/02-UI-SPEC.md
@.planning/phases/02-discovery-and-knowledge-brain/02-01-SUMMARY.md

@src/lib/agent-harness/types.ts
@src/lib/db.ts
@src/lib/safe-action.ts
@src/lib/project-scope.ts
@src/components/layout/sidebar.tsx
@src/components/layout/app-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat API route and conversation server actions</name>
  <files>
    src/app/api/chat/route.ts
    src/actions/conversations.ts
  </files>
  <action>
    1. Install AI SDK packages: `npm install ai @ai-sdk/anthropic`

    2. Install new shadcn components needed by chat: `npx shadcn@latest add scroll-area tooltip`

    3. Create `src/actions/conversations.ts` using next-safe-action pattern:
       - `getOrCreateGeneralChat(projectId)`: Find or create Conversation with conversationType="GENERAL" for project. Return conversation with recent messages (last 50).
       - `createConversation(projectId, type, title, metadata)`: Create new TASK_SESSION conversation. Return conversation object.
       - `getConversation(conversationId, projectId)`: Fetch conversation with all messages. Verify project scope.
       - `getConversations(projectId)`: List all conversations for project sidebar, ordered by updatedAt desc.
       - `saveMessage(conversationId, role, content, tokenUsage?, cost?)`: Persist a ChatMessage record. For AI messages, store inputTokens and outputTokens from the response.
       - All actions use scopedPrisma for project isolation.

    4. Create `src/app/api/chat/route.ts` per D-13:
       - Import `anthropic` from `@ai-sdk/anthropic`, `streamText` from `ai`
       - POST handler: extract { messages, projectId, conversationId } from request body
       - Authenticate via Clerk (use `auth()` from `@clerk/nextjs/server`)
       - Load project context for system prompt: call getProjectSummary + getOpenQuestions + getArticleSummaries from agent-harness context loaders
       - Build system prompt with project context (the AI should know about the project)
       - Call `streamText({ model: anthropic('claude-sonnet-4-20250514'), system, messages })`
       - Use `onFinish` callback to: save AI message to DB via saveMessage, record token usage
       - Return `result.toDataStreamResponse()`
       - Handle errors gracefully (return error message per UI-SPEC error states)
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/api/chat/route.ts src/actions/conversations.ts</automated>
  </verify>
  <done>Chat API route streams Claude responses via Vercel AI SDK, conversations persist to DB, all actions use project-scoped Prisma</done>
</task>

<task type="auto">
  <name>Task 2: Chat UI components with general and task-specific layouts</name>
  <files>
    src/components/chat/chat-interface.tsx
    src/components/chat/message-list.tsx
    src/components/chat/message-bubble.tsx
    src/components/chat/context-panel.tsx
    src/components/chat/token-display.tsx
    src/app/(dashboard)/projects/[projectId]/chat/page.tsx
    src/app/(dashboard)/projects/[projectId]/chat/[conversationId]/page.tsx
  </files>
  <action>
    1. Create `src/components/chat/message-bubble.tsx`:
       - Props: role ('user' | 'assistant' | 'system'), content (string), tokenUsage? ({ input, output }), cost? (number)
       - User bubble: `--muted` (#FAFAFA) background, `--foreground` text per UI-SPEC Chat Color Scheme
       - AI bubble: `--background` (#FFFFFF) with `--border` left border, `--foreground` text
       - System message: `--muted-foreground` text, no background, centered, 13px/400 metadata style
       - AI messages: render content as markdown (use a simple prose rendering approach)
       - Token/cost footer: 13px/400 muted metadata below AI messages (D-15). Format: "tokens: 450 | $0.004"
       - Chat bubble padding: 12px per UI-SPEC exception

    2. Create `src/components/chat/message-list.tsx`:
       - Props: messages array, isLoading boolean
       - Wrap in ScrollArea component (auto-scroll to bottom on new messages)
       - Group messages by date with separator ("Apr 6") using date-fns `format()`
       - Streaming indicator: three-dot animation in AI message bubble when isLoading
       - Map messages to MessageBubble components

    3. Create `src/components/chat/token-display.tsx`:
       - Props: totalTokens (number), totalCost (number)
       - Renders in chat header, right-aligned: "Session total: 12,400 tokens / $0.12"
       - 13px/400 muted style per UI-SPEC

    4. Create `src/components/chat/context-panel.tsx` per D-14:
       - Props: conversation (with related entities), sessionLog data
       - Fixed 320px width, right side
       - Sections: "Related Entities" (linked questions, decisions, transcripts as clickable links), "Token Usage" (prompt/response/session totals/cost)
       - Only shown for task-specific sessions, NOT general chat

    5. Create `src/components/chat/chat-interface.tsx` per D-12, D-13:
       - Props: conversationId, projectId, conversationType ('GENERAL' | 'TASK_SESSION'), initialMessages
       - Use `useChat` hook from `ai/react` with `api: '/api/chat'`, `body: { projectId, conversationId }`
       - Pass initialMessages from server-loaded conversation history
       - Chat input area: `--muted` background, 44px min-height, `--border` top border per UI-SPEC
       - Send button: accent icon button (ArrowUp lucide icon)
       - If conversationType === 'GENERAL': render full-width layout (MessageList only)
       - If conversationType === 'TASK_SESSION': render split layout (MessageList left + ContextPanel right)
       - Error handling: "Response interrupted. Click to retry." inline per UI-SPEC error states
       - Empty state per UI-SPEC: "Start a conversation" heading with placeholder text in input

    6. Create `src/app/(dashboard)/projects/[projectId]/chat/page.tsx`:
       - Server component that calls getOrCreateGeneralChat(projectId)
       - Renders ChatInterface with conversationType="GENERAL" and loaded messages
       - Page title "Project Chat" per UI-SPEC

    7. Create `src/app/(dashboard)/projects/[projectId]/chat/[conversationId]/page.tsx`:
       - Server component that calls getConversation(conversationId, projectId)
       - Renders ChatInterface with conversationType from conversation record
       - Split layout with ContextPanel for task sessions
       - TokenDisplay in header

    8. Update sidebar navigation to add "Chat" link with MessageSquare icon per UI-SPEC sidebar nav labels
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/chat/*.tsx src/app/\(dashboard\)/projects/\[projectId\]/chat/page.tsx</automated>
  </verify>
  <done>General chat page renders full-width with streaming responses, task session pages render split layout with context panel, messages persist across refresh, token/cost display works</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> /api/chat | User messages sent to streaming endpoint |
| /api/chat -> Claude | User + project context sent to AI |
| AI response -> client | Streaming AI content rendered in browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Spoofing | /api/chat route | mitigate | Authenticate via Clerk auth() before processing. Verify project membership. |
| T-02-10 | Tampering | /api/chat route | mitigate | Validate projectId ownership. Use scopedPrisma for all DB access. |
| T-02-11 | Information Disclosure | context assembly | mitigate | Context loaders only load data from the authenticated user's project. |
| T-02-12 | Tampering | message rendering | mitigate | AI-generated markdown rendered via safe prose styling. No dangerouslySetInnerHTML without DOMPurify. |
</threat_model>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. Chat page loads and renders empty state
3. Sidebar shows "Chat" nav item with MessageSquare icon
4. General chat and task session routes exist
</verification>

<success_criteria>
- Streaming chat works end-to-end with Claude via Vercel AI SDK
- General chat renders full-width, task sessions render split with context panel
- Messages persist to database and reload on page refresh
- Token/cost metadata shown per AI message and in session header
- Sidebar navigation updated with Chat link
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-and-knowledge-brain/02-03-SUMMARY.md`
</output>
