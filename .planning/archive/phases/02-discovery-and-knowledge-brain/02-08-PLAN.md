---
phase: 02-discovery-and-knowledge-brain
plan: 08
type: execute
wave: 5
depends_on: [02-04, 02-06]
files_modified:
  - src/lib/dashboard/queries.ts
  - src/lib/dashboard/usage-queries.ts
  - src/actions/dashboard.ts
  - src/lib/inngest/functions/dashboard-synthesis.ts
  - src/components/dashboard/health-score.tsx
  - src/components/dashboard/outstanding-questions.tsx
  - src/components/dashboard/blocked-items.tsx
  - src/components/dashboard/ai-summary-card.tsx
  - src/app/(dashboard)/projects/[projectId]/dashboard/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, KNOW-06]

must_haves:
  truths:
    - "Dashboard shows outstanding question counts by status and category as clickable stat cards"
    - "Dashboard shows blocked items with dependency chain indicators"
    - "Health score displays as percentage with descriptor (Good/Needs Attention/At Risk)"
    - "AI-generated 'Current Focus' and 'Recommended Focus' summaries displayed from cache"
    - "Dashboard data refreshes via Inngest-triggered cached synthesis, not live queries"
    - "AI identifies discovery gaps and assesses readiness (KNOW-06)"
  artifacts:
    - path: "src/lib/dashboard/queries.ts"
      provides: "Dashboard data queries — question stats, blocked items, health score"
      exports: ["getQuestionStats", "getBlockedItems", "computeHealthScore", "getCachedBriefing"]
    - path: "src/lib/inngest/functions/dashboard-synthesis.ts"
      provides: "Inngest function for AI-powered dashboard synthesis"
      exports: ["dashboardSynthesisFunction"]
    - path: "src/components/dashboard/health-score.tsx"
      provides: "Health score display with progress ring"
      exports: ["HealthScore"]
    - path: "src/app/(dashboard)/projects/[projectId]/dashboard/page.tsx"
      provides: "Discovery dashboard page"
  key_links:
    - from: "src/lib/inngest/functions/dashboard-synthesis.ts"
      to: "src/lib/agent-harness/engine.ts"
      via: "executeTask(dashboardSynthesisTask)"
      pattern: "executeTask.*dashboardSynthesisTask"
    - from: "src/app/(dashboard)/projects/[projectId]/dashboard/page.tsx"
      to: "src/lib/dashboard/queries.ts"
      via: "server component data loading"
      pattern: "getQuestionStats|getBlockedItems|computeHealthScore"
---

<objective>
Discovery dashboard: question stats, blocked items, health score, AI synthesis, and background refresh

Purpose: The dashboard is the team's situation awareness center -- at a glance, they see what questions are outstanding, what's blocked, how healthy discovery is, and what the AI recommends focusing on next. All data is pre-computed and cached (D-25) so the dashboard loads instantly. The AI proactively identifies discovery gaps (KNOW-06/D-20).

Output: Dashboard page with four sections (outstanding questions, blocked items, health score, AI summaries), Inngest synthesis function, and the data query layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-discovery-and-knowledge-brain/02-CONTEXT.md
@.planning/phases/02-discovery-and-knowledge-brain/02-RESEARCH.md
@.planning/phases/02-discovery-and-knowledge-brain/02-UI-SPEC.md

@SF-Consulting-AI-Framework-PRD-v2.1.md (Section 14: Discovery Dashboard)
@SESSION-3-TECHNICAL-SPEC.md (Section 5: Dashboard, Section 5.2: Health Score)

@src/lib/agent-harness/types.ts
@src/lib/agent-harness/engine.ts
@src/lib/agent-harness/tasks/dashboard-synthesis.ts
@src/lib/inngest/client.ts
@src/lib/inngest/events.ts
@src/lib/project-scope.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard data queries, health score, and usage queries</name>
  <files>
    src/lib/dashboard/queries.ts
    src/lib/dashboard/usage-queries.ts
    src/actions/dashboard.ts
  </files>
  <action>
    1. Create `src/lib/dashboard/queries.ts` per tech spec Section 5:
       - `getQuestionStats(projectId)` (DASH-01): Group questions by status, return counts per status (Open, Scoped, Owned, Answered, Reviewed). Also group by category for secondary breakdown.
       - `getBlockedItems(projectId)` (DASH-02): Query QuestionBlocksStory/Epic/Feature joins to find blocked items. Return list with: question displayId/title, blocked entity type/displayId/title, chain depth (how many levels deep the blocking goes).
       - `computeHealthScore(projectId)` (DASH-03, D-26): Derive from discovery completeness per tech spec Section 5.2:
         - Percentage of questions answered (weight: 40%)
         - Percentage of high-priority questions resolved (weight: 20%)
         - Open high-risk count (weight: 15%)
         - Knowledge article coverage — articles exist for key areas (weight: 15%)
         - Stale article count (weight: 10%)
         - Return: { score: number (0-100), descriptor: "Good" | "Needs Attention" | "At Risk", breakdown: { ... } }
         - Thresholds per UI-SPEC: >= 70% = "Good", 40-69% = "Needs Attention", < 40% = "At Risk"
       - `getCachedBriefing(projectId)` (DASH-04): Read pre-computed currentFocus and recommendedFocus from the project's cached briefing data (stored in a DashboardCache model or JSON field on Project).
       - All queries use scopedPrisma.

    2. Create `src/lib/dashboard/usage-queries.ts`:
       - `getTokenUsageSummary(projectId)`: Aggregate SessionLog records — total input/output tokens, total cost, breakdown by task type
       - Useful for the dashboard and individual session views

    3. Create `src/actions/dashboard.ts` using next-safe-action:
       - `getDashboardData(projectId)`: Calls all dashboard queries in parallel and returns combined result. Auth check + project scope validation.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/lib/dashboard/*.ts src/actions/dashboard.ts</automated>
  </verify>
  <done>Dashboard queries return question stats, blocked items, health score with descriptor, cached AI briefing, and token usage summary</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard UI components and Inngest synthesis function</name>
  <files>
    src/lib/inngest/functions/dashboard-synthesis.ts
    src/components/dashboard/health-score.tsx
    src/components/dashboard/outstanding-questions.tsx
    src/components/dashboard/blocked-items.tsx
    src/components/dashboard/ai-summary-card.tsx
    src/app/(dashboard)/projects/[projectId]/dashboard/page.tsx
  </files>
  <action>
    1. Create `src/lib/inngest/functions/dashboard-synthesis.ts` per D-25, DASH-05:
       - event: EVENTS.PROJECT_STATE_CHANGED
       - id: "dashboard-synthesis"
       - concurrency: { limit: 1, scope: "env", key: "event.data.projectId" }
       - debounce: { period: "30s" } — prevent rapid-fire synthesis when multiple entities change (pitfall 5)
       - Steps:
         a. `step.run("compute-metrics")`: Call getQuestionStats + getBlockedItems + computeHealthScore
         b. `step.run("synthesize")`: Call `executeTask(dashboardSynthesisTask, { userMessage: JSON.stringify(metrics) }, projectId, 'system')`. AI generates currentFocus (what to work on now) and recommendedFocus (discovery gaps per KNOW-06/D-20).
         c. `step.run("cache-results")`: Write synthesis results + computed metrics to DashboardCache. Update timestamp.
       - Register in Inngest route handler

    2. Create `src/components/dashboard/health-score.tsx` per D-26, UI-SPEC:
       - Props: score (number 0-100), descriptor (string)
       - Render Progress component (circular/ring style) with percentage in 24px/600
       - Descriptor text below: "Good", "Needs Attention", "At Risk" in 13px/600
       - Color: >= 70% accent (blue), 40-69% amber-500, < 40% destructive (red) per UI-SPEC thresholds

    3. Create `src/components/dashboard/outstanding-questions.tsx` per D-24, DASH-01:
       - Props: stats ({ open, scoped, owned, answeredAwaitingReview })
       - Four stat cards in a row: "Open: 5", "Scoped: 3", "Owned: 8", "Awaiting: 2"
       - Each card clickable — navigates to question list pre-filtered by that status
       - Use Card component from shadcn

    4. Create `src/components/dashboard/blocked-items.tsx` per DASH-02:
       - Props: blockedItems array
       - List of blocked items with dependency chain indicators (indented sub-items)
       - Each item shows: blocking question displayId + title, blocked entity type/displayId
       - Question links navigate to question detail page

    5. Create `src/components/dashboard/ai-summary-card.tsx` per D-24, D-20:
       - Props: title ("Current Focus" or "Recommended Focus"), content (markdown string), lastUpdated (Date)
       - Render markdown content safely (DOMPurify)
       - Skeleton loading state while data loads
       - "Last updated: X ago" in 13px muted below content

    6. Create `src/app/(dashboard)/projects/[projectId]/dashboard/page.tsx` per UI-SPEC Dashboard layout:
       - Server component loading data via getDashboardData
       - Layout per UI-SPEC exactly:
         - Page title "Discovery Dashboard"
         - Top row: HealthScore (left) + Current Focus card (right)
         - Middle: Outstanding Questions stat cards (full width)
         - Bottom row: Blocked Items (left) + Recommended Focus card (right)
       - All sections use Skeleton loading states
       - SWR polling with refreshInterval for periodic updates (D-25 — reads cached data, refresh happens via Inngest)
       - Empty state per UI-SPEC: "Discovery hasn't started" with "Process Transcript" accent button
       - Dashboard is read-only — no CTAs on the dashboard itself (just links to filtered views)

    7. Update sidebar: Add "Dashboard" nav item with LayoutDashboard icon per UI-SPEC
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/dashboard/*.tsx src/app/\(dashboard\)/projects/\[projectId\]/dashboard/page.tsx src/lib/inngest/functions/dashboard-synthesis.ts</automated>
  </verify>
  <done>Dashboard page renders all four sections with cached data, health score shows correct color/descriptor, AI summaries display markdown, Inngest synthesis function triggers on state changes, sidebar updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI synthesis -> cache | AI-generated focus summaries stored in dashboard cache |
| Cached content -> browser | AI markdown rendered in dashboard UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-28 | Tampering | ai-summary-card.tsx | mitigate | AI-generated markdown sanitized via DOMPurify before rendering |
| T-02-29 | Denial of Service | dashboard-synthesis | mitigate | Debounce (30s) prevents rapid-fire synthesis. Concurrency limit 1 per project. |
| T-02-30 | Information Disclosure | queries.ts | mitigate | All queries use scopedPrisma for project isolation |
</threat_model>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. Dashboard page loads with empty state
3. Health score renders with correct thresholds
4. Inngest function registered
5. SWR polling configured on dashboard
</verification>

<success_criteria>
- Dashboard shows outstanding questions by status as clickable stat cards
- Blocked items list with dependency chains
- Health score percentage with color-coded descriptor
- AI Current Focus and Recommended Focus cards from cached synthesis
- Inngest-triggered synthesis refreshes cache on project state changes with 30s debounce
- Discovery gap identification (KNOW-06) in Recommended Focus
- Sidebar shows Dashboard nav item
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-and-knowledge-brain/02-08-SUMMARY.md`
</output>
