---
phase: 10-chat-session-management-and-conversation-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/actions/conversations.ts
  - src/components/chat/chat-layout.tsx
  - src/components/chat/conversation-sidebar.tsx
  - src/components/chat/conversation-row.tsx
  - src/components/chat/conversation-filters.tsx
  - src/components/chat/session-status-badge.tsx
  - src/components/chat/new-session-dropdown.tsx
  - src/app/(dashboard)/projects/[projectId]/chat/layout.tsx
  - src/app/(dashboard)/projects/[projectId]/chat/page.tsx
  - src/app/(dashboard)/projects/[projectId]/chat/[conversationId]/page.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-12

must_haves:
  truths:
    - "User sees a left sidebar with conversation list alongside the chat area"
    - "General chat is pinned at top of the conversation sidebar"
    - "User can filter conversations by tab: All, Stories, Briefings, Transcripts, Questions"
    - "User can search conversations by title text"
    - "User can archive a conversation and it disappears from default list"
    - "User can toggle archived view to see archived conversations"
    - "Conversation rows show session type icon, title, timestamp, and status badge"
    - "User can rename a conversation inline from hover action"
    - "New Session dropdown shows available session types"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "isArchived Boolean field on Conversation model"
      contains: "isArchived"
    - path: "src/components/chat/chat-layout.tsx"
      provides: "Split layout with sidebar (320px) + chat area (flex-1)"
      min_lines: 20
    - path: "src/components/chat/conversation-sidebar.tsx"
      provides: "Left panel with conversation list, search, tabs, pinned general chat"
      min_lines: 80
    - path: "src/components/chat/conversation-row.tsx"
      provides: "Single row: icon, title, timestamp, status badge, hover actions"
      min_lines: 40
    - path: "src/components/chat/conversation-filters.tsx"
      provides: "Tab bar + search input for filtering"
      min_lines: 30
    - path: "src/components/chat/session-status-badge.tsx"
      provides: "ACTIVE/COMPLETE/FAILED badge"
      min_lines: 15
    - path: "src/components/chat/new-session-dropdown.tsx"
      provides: "+ New button with dropdown type picker"
      min_lines: 30
    - path: "src/actions/conversations.ts"
      provides: "archiveConversation, unarchiveConversation, renameConversation actions"
      exports: ["archiveConversation", "unarchiveConversation", "renameConversation"]
  key_links:
    - from: "src/app/(dashboard)/projects/[projectId]/chat/layout.tsx"
      to: "src/components/chat/chat-layout.tsx"
      via: "import and render"
      pattern: "ChatLayout"
    - from: "src/components/chat/conversation-sidebar.tsx"
      to: "src/actions/conversations.ts"
      via: "getConversations for loading list"
      pattern: "getConversations"
    - from: "src/components/chat/conversation-row.tsx"
      to: "src/actions/conversations.ts"
      via: "archive and rename actions"
      pattern: "archiveConversation|renameConversation"
---

<objective>
Build the conversation sidebar UI, chat layout restructure, and conversation lifecycle server actions (archive, rename).

Purpose: Provides the ChatGPT-style sidebar navigation that all subsequent session types will render into. Also adds the isArchived schema field and lifecycle server actions that the sidebar depends on.

Output: Chat page has a left sidebar (320px) with filterable conversation list, pinned general chat, search, tab filters, hover actions (rename, archive), and a "+ New" dropdown. Schema updated with isArchived field.
</objective>

<execution_context>
@/Users/michaelrihm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelrihm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-chat-session-management-and-conversation-intelligence/10-CONTEXT.md
@.planning/phases/10-chat-session-management-and-conversation-intelligence/10-RESEARCH.md
@.planning/phases/10-chat-session-management-and-conversation-intelligence/10-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From prisma/schema.prisma:
```prisma
enum ConversationType {
  GENERAL_CHAT
  TRANSCRIPT_SESSION
  STORY_SESSION
  BRIEFING_SESSION
  QUESTION_SESSION
  ENRICHMENT_SESSION
}

enum ConversationStatus {
  ACTIVE
  COMPLETE
  FAILED
}

model Conversation {
  id               String             @id @default(cuid())
  projectId        String
  conversationType ConversationType
  title            String?
  status           ConversationStatus @default(ACTIVE)
  createdById      String
  sessionLogId     String?
  metadata         Json?
  createdAt        DateTime           @default(now())
  updatedAt        DateTime           @updatedAt
  // NO isArchived field yet — Task 1 adds it
}
```

From src/actions/conversations.ts:
```typescript
export const getOrCreateGeneralChat = actionClient.schema(...).action(...)
export const createConversation = actionClient.schema(...).action(...)
export const getConversation = actionClient.schema(...).action(...)
export const getConversations = actionClient.schema(...).action(...)
export const getSessionTokenTotals = actionClient.schema(...).action(...)
export const initiateStorySession = actionClient.schema(...).action(...)
export const saveMessage = actionClient.schema(...).action(...)
```

From src/lib/safe-action.ts (action pattern):
```typescript
export const actionClient = createSafeActionClient({ ... })
```

From src/lib/project-scope.ts:
```typescript
export function scopedPrisma(projectId: string) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration + conversation lifecycle server actions</name>
  <files>prisma/schema.prisma, src/actions/conversations.ts</files>
  <read_first>
    - prisma/schema.prisma (Conversation model around line 1057)
    - src/actions/conversations.ts (all existing actions, patterns)
    - src/lib/safe-action.ts (actionClient import)
    - src/lib/project-scope.ts (scopedPrisma pattern)
    - src/lib/auth.ts (getCurrentMember pattern)
  </read_first>
  <action>
1. In `prisma/schema.prisma`, add to the Conversation model:
   ```
   isArchived       Boolean            @default(false)
   ```
   Place it after the `metadata` field, before `createdAt`.

2. In `src/actions/conversations.ts`, modify the existing `getConversations` action to add `isArchived: false` as a default filter. Add an optional `includeArchived` boolean input to the schema:
   ```typescript
   const getConversationsSchema = z.object({
     projectId: z.string().min(1),
     includeArchived: z.boolean().optional(),
   })
   ```
   In the findMany where clause: `where: { ...(includeArchived ? {} : { isArchived: false }) }`

3. Add three new server actions to `src/actions/conversations.ts`:

   **archiveConversation:**
   - Schema: `{ projectId: z.string().min(1), conversationId: z.string().min(1) }`
   - Calls `getCurrentMember(projectId)` for auth
   - Uses `scopedPrisma(projectId)` to verify conversation belongs to project via `findFirst`
   - Updates `isArchived: true` on the conversation
   - Returns the updated conversation

   **unarchiveConversation:**
   - Same pattern as archive but sets `isArchived: false`

   **renameConversation:**
   - Schema: `{ projectId: z.string().min(1), conversationId: z.string().min(1), title: z.string().min(1).max(100) }`
   - Same auth + scoping pattern
   - Updates `title` on the conversation
   - Returns the updated conversation
  </action>
  <verify>
    <automated>npx prisma validate && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `isArchived       Boolean            @default(false)` inside the Conversation model
    - src/actions/conversations.ts contains `export const archiveConversation`
    - src/actions/conversations.ts contains `export const unarchiveConversation`
    - src/actions/conversations.ts contains `export const renameConversation`
    - src/actions/conversations.ts getConversations where clause includes `isArchived` filter
    - `npx prisma validate` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Schema has isArchived field. Three new server actions (archive, unarchive, rename) exist and type-check. getConversations filters out archived by default.</done>
</task>

<task type="auto">
  <name>Task 2: Conversation sidebar UI components and chat layout</name>
  <files>src/components/chat/chat-layout.tsx, src/components/chat/conversation-sidebar.tsx, src/components/chat/conversation-row.tsx, src/components/chat/conversation-filters.tsx, src/components/chat/session-status-badge.tsx, src/components/chat/new-session-dropdown.tsx, src/app/(dashboard)/projects/[projectId]/chat/layout.tsx, src/app/(dashboard)/projects/[projectId]/chat/page.tsx, src/app/(dashboard)/projects/[projectId]/chat/[conversationId]/page.tsx</files>
  <read_first>
    - src/components/chat/chat-interface.tsx (current ChatInterface component, understand props)
    - src/app/(dashboard)/projects/[projectId]/chat/page.tsx (current general chat page)
    - src/app/(dashboard)/projects/[projectId]/chat/[conversationId]/page.tsx (current session page)
    - src/actions/conversations.ts (getConversations, archiveConversation, renameConversation from Task 1)
    - src/components/chat/token-display.tsx (existing token display pattern)
    - .planning/phases/10-chat-session-management-and-conversation-intelligence/10-UI-SPEC.md (full visual contract)
    - src/components/app-shell.tsx (existing layout pattern for sidebar reference)
  </read_first>
  <action>
1. **Create `src/components/chat/session-status-badge.tsx`** (per D-11 UI-SPEC):
   - Props: `{ status: "ACTIVE" | "COMPLETE" | "FAILED"; conversationType?: string }`
   - ACTIVE: Badge variant="outline" text "Active" — only shown for task sessions (not GENERAL_CHAT)
   - COMPLETE: Badge variant="secondary" text "Complete"
   - FAILED: custom `bg-destructive/10 text-destructive border-destructive/20` text "Failed"
   - Use shadcn Badge component

2. **Create `src/components/chat/conversation-filters.tsx`** (per D-02, D-03):
   - Client component ("use client")
   - Props: `{ activeTab: string; onTabChange: (tab: string) => void; searchQuery: string; onSearchChange: (query: string) => void }`
   - Renders shadcn Tabs with TabsList and TabsTrigger for: "all", "stories", "briefings", "transcripts", "questions"
   - Below tabs: shadcn Input with Search lucide icon (16px) as left adornment, placeholder "Search conversations..."
   - Tab text 13px. Active tab uses accent underline.

3. **Create `src/components/chat/conversation-row.tsx`** (per D-04):
   - Client component
   - Props: `{ conversation: ConversationListItem; isSelected: boolean; isPinned?: boolean; onArchive: (id: string) => void; onRename: (id: string, title: string) => void }`
   - Type `ConversationListItem`: `{ id: string; conversationType: string; title: string | null; status: string; updatedAt: Date | string; isArchived: boolean }`
   - Left: session type icon (16px, text-muted-foreground) — map ConversationType to lucide icons:
     GENERAL_CHAT -> MessageSquare, STORY_SESSION -> BookOpen, TRANSCRIPT_SESSION -> FileText, BRIEFING_SESSION -> LayoutDashboard, QUESTION_SESSION -> HelpCircle, ENRICHMENT_SESSION -> Sparkles
   - Center: title (14px, truncate), relative timestamp line (13px text-muted-foreground) using date-fns `formatDistanceToNow`
   - Right: SessionStatusBadge for non-ACTIVE sessions
   - Selected: `bg-background border-l-2 border-primary`
   - Hover: `bg-background/80`, reveals action icons (Pencil, Archive) at 14px text-muted-foreground
   - Right-click: shadcn DropdownMenu with "Rename" and "Archive" (or "Unarchive" if archived)
   - Inline rename: on pencil click, title becomes Input, Enter saves via onRename callback, Escape cancels
   - Wrap row in Next.js `Link` to `/projects/${projectId}/chat/${conversation.id}` (or `/chat` for general)

4. **Create `src/components/chat/new-session-dropdown.tsx`** (per D-05):
   - Client component
   - Props: `{ projectId: string }`
   - Button variant="default" size="sm" with Plus icon + "New Session" text
   - DropdownMenu with items:
     - "Story Generation" (BookOpen icon) — description "Generate stories from epic context"
     - "Briefing" (LayoutDashboard icon) — description "Generate a project briefing"
     - "Question Impact" (HelpCircle icon) — description "Assess question impact"
     - "Story Enrichment" (Sparkles icon) — description "Enrich a story with AI suggestions"
   - Each item: icon (16px) + label (14px) + description (13px text-muted-foreground)
   - On click: for "Briefing", call `createConversation` with type BRIEFING_SESSION and navigate to the new session URL. For others that need entity selection, use `router.push` to navigate to chat with search params `?newSession=STORY_SESSION` (entity selection will be handled in Plan 02/03 via the chat page).

5. **Create `src/components/chat/conversation-sidebar.tsx`** (per D-01):
   - Client component ("use client")
   - Props: `{ projectId: string; conversations: ConversationListItem[]; currentConversationId?: string }`
   - State: `activeTab` (default "all"), `searchQuery` (default ""), `showArchived` (default false)
   - Uses ConversationFilters for tab bar + search
   - Filters conversations client-side: by tab (map tab to ConversationType), by search query (case-insensitive match on title), by archived status
   - General chat (GENERAL_CHAT type) is pinned at top, separated by Separator
   - Session list below, sorted by updatedAt DESC
   - At bottom: "Show Archived" toggle link (13px text-muted-foreground). When toggled, loads archived conversations and shows at 50% opacity.
   - Archive action calls archiveConversation server action + router.refresh()
   - Rename action calls renameConversation server action + router.refresh()
   - Background: `bg-muted`, border-right: `border-border`, width: `w-80` (320px)
   - Scrollable via shadcn ScrollArea

6. **Create `src/components/chat/chat-layout.tsx`**:
   - Server component
   - Props: `{ projectId: string; conversations: ConversationListItem[]; currentConversationId?: string; children: React.ReactNode }`
   - Renders: `<div className="flex h-full">` with ConversationSidebar (left, w-80) and children (right, flex-1)

7. **Create `src/app/(dashboard)/projects/[projectId]/chat/layout.tsx`**:
   - Server component
   - Receives `params.projectId` and `children`
   - Calls `getCurrentMember(projectId)` for auth
   - Calls `getConversations({ projectId })` to load conversation list
   - Renders `<ChatLayout projectId={projectId} conversations={conversations} currentConversationId={derived from children context or undefined}>{children}</ChatLayout>`
   - Note: since layout.tsx cannot know the conversationId from child route params, pass undefined. The sidebar will derive selection from the URL using `usePathname()`.

8. **Update `src/app/(dashboard)/projects/[projectId]/chat/page.tsx`**:
   - Remove any layout wrapper if present (layout.tsx now handles it)
   - Keep the ChatInterface rendering for general chat
   - The page just renders the ChatInterface content — sidebar is handled by layout.tsx

9. **Update `src/app/(dashboard)/projects/[projectId]/chat/[conversationId]/page.tsx`**:
   - Same approach — remove any duplicate layout, page just renders ChatInterface for the specific session
   - Sidebar handled by layout.tsx
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/chat/chat-layout.tsx exists and contains `ConversationSidebar`
    - src/components/chat/conversation-sidebar.tsx exists and contains `"use client"` and `ConversationFilters` and `ConversationRow` and `ScrollArea`
    - src/components/chat/conversation-row.tsx exists and contains `MessageSquare` and `BookOpen` and `LayoutDashboard` and `HelpCircle` and `Sparkles` and `formatDistanceToNow`
    - src/components/chat/conversation-filters.tsx exists and contains `TabsTrigger` and `"all"` and `"stories"` and `"briefings"` and `"transcripts"` and `"questions"` and `Search`
    - src/components/chat/session-status-badge.tsx exists and contains `ACTIVE` and `COMPLETE` and `FAILED`
    - src/components/chat/new-session-dropdown.tsx exists and contains `"New Session"` and `Plus` and `DropdownMenu`
    - src/app/(dashboard)/projects/[projectId]/chat/layout.tsx exists and contains `ChatLayout` and `getConversations`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Chat page renders with a 320px left sidebar showing filtered, searchable conversation list with pinned general chat, session type icons, status badges, hover actions (rename/archive), and a "+ New" dropdown. Layout shared across all chat routes via layout.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server Actions | Archive/rename/unarchive actions receive conversationId from client |
| URL Routing | ConversationId in URL determines which conversation loads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | getConversations | mitigate | scopedPrisma(projectId) ensures cross-project isolation. Already verified in existing action. |
| T-10-02 | Tampering | archiveConversation | mitigate | getCurrentMember(projectId) verifies project membership before mutation. scopedPrisma findFirst verifies conversation belongs to project. |
| T-10-03 | Tampering | renameConversation | mitigate | Same auth + scoping as T-10-02. Title input validated by Zod: z.string().min(1).max(100). |
| T-10-04 | Elevation of Privilege | URL routing | accept | Any project member can view any conversation in their project. No per-conversation ACL needed — all members share project context. |
</threat_model>

<verification>
1. `npx prisma validate` — schema is valid
2. `npx tsc --noEmit` — all files type-check
3. Visual: navigate to /projects/{id}/chat and see sidebar with conversation list
</verification>

<success_criteria>
Chat page has a left sidebar (320px) with filterable, searchable conversation list. General chat pinned at top. Session type icons, status badges, hover actions (rename, archive), "+ New" dropdown all functional. Schema has isArchived field. getConversations filters archived by default.
</success_criteria>

<output>
After completion, create `.planning/phases/10-chat-session-management-and-conversation-intelligence/10-01-SUMMARY.md`
</output>
