---
phase: 10-chat-session-management-and-conversation-intelligence
plan: 05
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03", "10-04"]
files_modified:
  - prisma/schema.prisma
autonomous: false
requirements:
  - D-01
  - D-06
  - D-12
  - D-13

must_haves:
  truths:
    - "Database schema matches Prisma schema (isArchived field exists in DB)"
    - "Application builds successfully"
    - "All chat pages render without errors"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Final schema with isArchived field pushed to database"
      contains: "isArchived"
  key_links:
    - from: "prisma/schema.prisma"
      to: "database"
      via: "prisma db push"
      pattern: "isArchived"
---

<objective>
Push the Prisma schema to the database and verify the full phase works end-to-end.

Purpose: The isArchived field added in Plan 01 must be pushed to the live database. Without this, archive/unarchive actions will fail at runtime even though TypeScript compiles fine. Also verifies all plans integrate correctly.

Output: Database schema in sync with Prisma schema. Full chat system verified functional.
</objective>

<execution_context>
@/Users/michaelrihm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelrihm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-chat-session-management-and-conversation-intelligence/10-CONTEXT.md
@.planning/phases/10-chat-session-management-and-conversation-intelligence/10-01-SUMMARY.md
@.planning/phases/10-chat-session-management-and-conversation-intelligence/10-02-SUMMARY.md
@.planning/phases/10-chat-session-management-and-conversation-intelligence/10-03-SUMMARY.md
@.planning/phases/10-chat-session-management-and-conversation-intelligence/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push Prisma schema to database</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (verify isArchived field is present on Conversation model)
  </read_first>
  <action>
Run `npx prisma db push` to sync the schema to the database.

If DATABASE_URL is not set in the local environment (it was not available in Phase 6), this push must be deferred. Check for DATABASE_URL:
```bash
grep DATABASE_URL .env.local 2>/dev/null || echo "NO_DATABASE_URL"
```

If DATABASE_URL is present: run `npx prisma db push`
If DATABASE_URL is absent: log that schema push is deferred (same pattern as Phase 6 decision). The schema validates and Prisma client generates correctly without the push — it will be applied on first deployment to Neon.

Also run `npx prisma generate` to ensure the Prisma client reflects the schema changes.
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma generate && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` exits 0
    - `npx prisma generate` exits 0
    - `npx tsc --noEmit` exits 0
    - If DATABASE_URL present: `npx prisma db push` exits 0
    - If DATABASE_URL absent: noted as deferred (consistent with Phase 6 pattern)
  </acceptance_criteria>
  <done>Prisma schema validated and client generated. Database push completed or deferred per environment availability.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 10 chat system end-to-end</name>
  <what-built>Complete Phase 10 chat system: conversation sidebar with filtering/search/archive, three new session types (BRIEFING, ENRICHMENT, QUESTION), session lifecycle (auto-complete, retry, read-only), and smart context retrieval for general chat.</what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev`
2. Navigate to any project's chat page (e.g., http://localhost:3000/projects/{id}/chat)
3. Verify the left sidebar (320px) shows:
   - Pinned General Chat at top
   - Tab bar: All | Stories | Briefings | Transcripts | Questions
   - Search bar
   - "+ New Session" button with dropdown
   - Existing conversations sorted by recency
4. Click a conversation row — verify it loads in the chat area
5. Hover a conversation row — verify rename/archive icons appear
6. Click archive on a conversation — verify it disappears. Toggle "Show Archived" to verify it reappears.
7. Navigate to the discovery dashboard, verify "Generate Briefing" button is present
8. Navigate to a story detail page, verify "Enrich with AI" button is present
9. In general chat, ask a question about the project — verify AI response references more than just open questions (should mention stories, org knowledge, etc.)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma -> Database | Schema push modifies live database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-16 | Denial of Service | prisma db push | accept | Non-destructive change (adding nullable boolean with default). No data loss risk. |
</threat_model>

<verification>
1. `npx prisma validate` — schema valid
2. `npx tsc --noEmit` — full build type-checks
3. Human verifies chat sidebar, session types, and smart context
</verification>

<success_criteria>
Schema pushed to database (or deferred). Full Phase 10 verified by human: sidebar renders with conversations, filtering works, archive works, entry point buttons present, general chat shows intelligence from all project data sources.
</success_criteria>

<output>
After completion, create `.planning/phases/10-chat-session-management-and-conversation-intelligence/10-05-SUMMARY.md`
</output>
