---
phase: 06-schema-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/actions/projects.ts
  - src/actions/questions.ts
  - src/app/(dashboard)/projects/[projectId]/settings/page.tsx
  - src/components/projects/project-settings-form.tsx
  - src/components/questions/question-form.tsx
  - src/components/questions/question-table.tsx
  - src/app/(dashboard)/projects/[projectId]/questions/page.tsx
  - src/app/(dashboard)/projects/[projectId]/questions/questions-page-client.tsx
autonomous: true
requirements:
  - PROJ-01
  - QUES-08
must_haves:
  truths:
    - "sandboxStrategy value entered in create wizard is persisted to the database and visible on settings page reload"
    - "sandboxStrategy value edited on settings page is persisted to the database"
    - "User can select a category when creating a question"
    - "Category filter dropdown appears in question table and filters questions by category"
    - "Question table displays category column for each question"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "sandboxStrategy field on Project model, QuestionCategory enum, category field on Question model"
      contains: "sandboxStrategy"
    - path: "src/actions/projects.ts"
      provides: "sandboxStrategy persisted in create and update"
      contains: "sandboxStrategy"
    - path: "src/actions/questions.ts"
      provides: "category field in create/update schemas and getQuestions filter"
      contains: "category"
    - path: "src/components/questions/question-table.tsx"
      provides: "Category filter dropdown and category column"
      contains: "categoryFilter"
  key_links:
    - from: "src/actions/projects.ts createProject"
      to: "prisma.project.create"
      via: "sandboxStrategy in data object"
      pattern: "sandboxStrategy.*parsedInput\\.sandboxStrategy"
    - from: "src/actions/projects.ts updateProject"
      to: "prisma.project.update"
      via: "sandboxStrategy in updateData"
      pattern: "updateData\\.sandboxStrategy"
    - from: "src/components/questions/question-table.tsx"
      to: "URL search params"
      via: "nuqs useQueryState for category"
      pattern: "useQueryState.*category"
---

<objective>
Fix two data model gaps from the v1.0 audit: (1) sandboxStrategy field silently discarded on Project create/update, and (2) Question model missing category field preventing category-based filtering.

Purpose: Close PROJ-01 and QUES-08 requirements that were flagged as unsatisfied/partial in the milestone audit.
Output: Updated Prisma schema with both fields, server actions persisting both fields, UI for category selection and filtering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prisma/schema.prisma
@src/actions/projects.ts
@src/actions/questions.ts
@src/components/questions/question-table.tsx
@src/components/questions/question-form.tsx
@src/app/(dashboard)/projects/[projectId]/questions/page.tsx
@src/app/(dashboard)/projects/[projectId]/questions/questions-page-client.tsx
@src/app/(dashboard)/projects/[projectId]/settings/page.tsx
@src/components/projects/project-settings-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sandboxStrategy to Project model and wire through actions</name>
  <files>prisma/schema.prisma, src/actions/projects.ts, src/app/(dashboard)/projects/[projectId]/settings/page.tsx</files>
  <read_first>
    - prisma/schema.prisma (lines 394-443 for Project model)
    - src/actions/projects.ts (full file - 148 lines)
    - src/app/(dashboard)/projects/[projectId]/settings/page.tsx (full file)
    - src/components/projects/project-settings-form.tsx (full file)
  </read_first>
  <action>
1. In prisma/schema.prisma, add `sandboxStrategy String?` to the Project model after the `targetEndDate` field (line ~401, after `targetEndDate DateTime?`). This is an optional free-text field matching the Textarea in the wizard UI.

2. In src/actions/projects.ts, fix the `createProject` action (line ~45-57): Add `sandboxStrategy: parsedInput.sandboxStrategy ?? null,` to the `prisma.project.create({ data: {...} })` call, after the `currentPhase` line.

3. In src/actions/projects.ts, fix the `updateProject` action (line ~127-143): Add the line `if (parsedInput.sandboxStrategy !== undefined) updateData.sandboxStrategy = parsedInput.sandboxStrategy || null` after the `targetEndDate` handling block (after line ~139).

4. In src/app/(dashboard)/projects/[projectId]/settings/page.tsx, fix the hardcoded `sandboxStrategy: ""` (line ~70). Change it to `sandboxStrategy: project.sandboxStrategy ?? ""`. This requires that the `project` query on that page already includes all scalar fields (it does via default Prisma select).
  </action>
  <verify>
    <automated>cd /Users/michaelrihm/Documents/Salesforce-Consulting-SDLC-App && grep -n "sandboxStrategy" prisma/schema.prisma | grep -v "//" && grep -n "sandboxStrategy.*parsedInput" src/actions/projects.ts && grep -n "sandboxStrategy.*project\." src/app/\(dashboard\)/projects/\[projectId\]/settings/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `sandboxStrategy  String?` inside model Project block (between targetEndDate and status lines)
    - src/actions/projects.ts createProject data block contains `sandboxStrategy: parsedInput.sandboxStrategy`
    - src/actions/projects.ts updateProject contains `updateData.sandboxStrategy = parsedInput.sandboxStrategy`
    - src/app/(dashboard)/projects/[projectId]/settings/page.tsx contains `sandboxStrategy: project.sandboxStrategy` (not hardcoded empty string)
  </acceptance_criteria>
  <done>sandboxStrategy field exists on Project model, is persisted through both create and update actions, and settings page reads from database</done>
</task>

<task type="auto">
  <name>Task 2: Add QuestionCategory enum, category field, form input, and table filter</name>
  <files>prisma/schema.prisma, src/actions/questions.ts, src/components/questions/question-form.tsx, src/components/questions/question-table.tsx, src/app/(dashboard)/projects/[projectId]/questions/page.tsx, src/app/(dashboard)/projects/[projectId]/questions/questions-page-client.tsx</files>
  <read_first>
    - prisma/schema.prisma (lines 94-108 for QuestionScope/QuestionStatus enums, lines 587-622 for Question model)
    - src/actions/questions.ts (full file)
    - src/components/questions/question-form.tsx (full file)
    - src/components/questions/question-table.tsx (full file)
    - src/app/(dashboard)/projects/[projectId]/questions/page.tsx (full file)
    - src/app/(dashboard)/projects/[projectId]/questions/questions-page-client.tsx (full file)
  </read_first>
  <action>
1. In prisma/schema.prisma, add a new enum after QuestionScope (after line 98):

```
enum QuestionCategory {
  BUSINESS_PROCESS
  TECHNICAL
  DATA
  INTEGRATION
  SECURITY
  COMPLIANCE
  DESIGN
  GENERAL
}
```

2. In prisma/schema.prisma, add `category QuestionCategory @default(GENERAL)` to the Question model after the `scope` field (after line 592). Using a default so existing rows are valid.

3. In src/actions/questions.ts, update the `createQuestionSchema` (line ~29): Add `category: z.enum(["BUSINESS_PROCESS", "TECHNICAL", "DATA", "INTEGRATION", "SECURITY", "COMPLIANCE", "DESIGN", "GENERAL"]).default("GENERAL"),` after the `scope` field.

4. In src/actions/questions.ts, update the `updateQuestionSchema` (line ~39): Add `category: z.enum(["BUSINESS_PROCESS", "TECHNICAL", "DATA", "INTEGRATION", "SECURITY", "COMPLIANCE", "DESIGN", "GENERAL"]).optional(),` after the `scope` field.

5. In src/actions/questions.ts, update the `getQuestionsSchema` (line ~69): Add `category: z.enum(["BUSINESS_PROCESS", "TECHNICAL", "DATA", "INTEGRATION", "SECURITY", "COMPLIANCE", "DESIGN", "GENERAL"]).optional(),` after the `priority` field.

6. In src/actions/questions.ts, in the `createQuestion` action (line ~100-112), add `category: parsedInput.category,` to the `prisma.question.create({ data: {...} })` call.

7. In src/actions/questions.ts, in the `updateQuestion` action (line ~154-162), add: `if (updateFields.category !== undefined) updateData.category = updateFields.category` in the updateData block.

8. In src/actions/questions.ts, in the `getQuestions` action (line ~292-299), add: `if (parsedInput.category) where.category = parsedInput.category` in the where clause block.

9. In src/components/questions/question-form.tsx, add a Category select field to the form:
   - Add `category: z.enum(["BUSINESS_PROCESS", "TECHNICAL", "DATA", "INTEGRATION", "SECURITY", "COMPLIANCE", "DESIGN", "GENERAL"]).default("GENERAL"),` to questionFormSchema (after the priority field).
   - Add `category: "GENERAL"` to defaultValues.
   - Add a Category Select dropdown between the Priority and Assignee sections, with display labels: "Business Process", "Technical", "Data", "Integration", "Security", "Compliance", "Design", "General". Use the same Select pattern as the Priority field. Use `form.watch("category")` for value and `form.setValue("category", v as ...)` for onValueChange.

10. In src/components/questions/question-table.tsx:
    - Add `category?: string` to the QuestionRow interface.
    - Add a CATEGORY_LABELS map: `{ BUSINESS_PROCESS: "Business Process", TECHNICAL: "Technical", DATA: "Data", INTEGRATION: "Integration", SECURITY: "Security", COMPLIANCE: "Compliance", DESIGN: "Design", GENERAL: "General" }`.
    - Add a nuqs state: `const [categoryFilter, setCategoryFilter] = useQueryState("category", parseAsString)`.
    - Add a Category filter Select dropdown in the filter bar (after the Priority filter), with "All Categories" as default and all 8 enum values as options. Follow the same pattern as the existing status/scope/priority filters.
    - Add a "Category" column header in the table between the "Scope" and "Owner" columns.
    - Add a TableCell for category in each row: `<TableCell className="text-[13px] text-[#737373]">{CATEGORY_LABELS[q.category ?? ""] ?? q.category ?? ""}</TableCell>`.
    - Update the empty-state TableCell colSpan from 7 to 8.

11. In src/app/(dashboard)/projects/[projectId]/questions/page.tsx, add category filter to the where clause (after the priority line ~31): `if (search.category && typeof search.category === "string") where.category = search.category`.

12. In src/app/(dashboard)/projects/[projectId]/questions/questions-page-client.tsx, add `category?: string` to the QuestionRow interface.
  </action>
  <verify>
    <automated>cd /Users/michaelrihm/Documents/Salesforce-Consulting-SDLC-App && grep -n "QuestionCategory" prisma/schema.prisma && grep -n "category" src/actions/questions.ts | head -10 && grep -n "categoryFilter" src/components/questions/question-table.tsx && grep -n "category" src/components/questions/question-form.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `enum QuestionCategory` with 8 values (BUSINESS_PROCESS, TECHNICAL, DATA, INTEGRATION, SECURITY, COMPLIANCE, DESIGN, GENERAL)
    - prisma/schema.prisma Question model contains `category QuestionCategory @default(GENERAL)`
    - src/actions/questions.ts createQuestionSchema contains `category: z.enum(`
    - src/actions/questions.ts createQuestion action data block contains `category: parsedInput.category`
    - src/actions/questions.ts getQuestionsSchema contains `category:`
    - src/actions/questions.ts getQuestions where clause contains `parsedInput.category`
    - src/components/questions/question-form.tsx contains a Select with value "BUSINESS_PROCESS" (at minimum)
    - src/components/questions/question-table.tsx contains `categoryFilter` via useQueryState
    - src/components/questions/question-table.tsx contains `CATEGORY_LABELS` map
    - src/components/questions/question-table.tsx Category column header exists
    - src/app/(dashboard)/projects/[projectId]/questions/page.tsx where clause contains `search.category`
    - src/app/(dashboard)/projects/[projectId]/questions/questions-page-client.tsx QuestionRow has `category`
  </acceptance_criteria>
  <done>QuestionCategory enum exists, category field on Question model with default, category in create/update/filter actions, category select in question form, category filter and column in question table</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push schema to database and verify build</name>
  <files></files>
  <read_first>
    - prisma/schema.prisma (verify sandboxStrategy and QuestionCategory changes are present)
  </read_first>
  <action>
1. Run `npx prisma db push` to push the schema changes to the database. If the command prompts for confirmation about data loss (it should not since we are only adding nullable/defaulted fields), use `npx prisma db push --accept-data-loss`.

2. Run `npx prisma generate` to regenerate the Prisma client with the new types.

3. Run `npx next build` (or `npx tsc --noEmit` if build is too slow) to verify TypeScript compilation passes with the new schema fields wired through actions and components.
  </action>
  <verify>
    <automated>cd /Users/michaelrihm/Documents/Salesforce-Consulting-SDLC-App && npx prisma db push --accept-data-loss 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` exits with code 0 and outputs "Your database is now in sync with your Prisma schema"
    - `npx tsc --noEmit` exits with code 0 (no type errors)
  </acceptance_criteria>
  <done>Database schema is synced with Prisma schema, TypeScript compiles without errors, both new fields are live in the database</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form -> server action | sandboxStrategy and category values cross from untrusted client input to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | createProject, updateProject | mitigate | sandboxStrategy validated as z.string().optional() by Zod in server action -- free text, no injection risk since Prisma parameterizes queries |
| T-06-02 | T (Tampering) | createQuestion, updateQuestion | mitigate | category validated as z.enum([...]) by Zod -- only the 8 defined enum values accepted, invalid values rejected before DB |
| T-06-03 | I (Information Disclosure) | getQuestions filter | accept | category filter uses enum-validated value in Prisma where clause -- no risk of SQL injection or data leakage beyond project scope (scopedPrisma enforces project isolation) |
</threat_model>

<verification>
1. Prisma schema has sandboxStrategy on Project and category/QuestionCategory on Question
2. `npx prisma db push` succeeds
3. `npx tsc --noEmit` passes
4. createProject action includes sandboxStrategy in data
5. updateProject action includes sandboxStrategy in updateData
6. Settings page reads sandboxStrategy from project object
7. createQuestion includes category in data
8. getQuestions filters by category
9. Question form has Category dropdown
10. Question table has Category filter and column
</verification>

<success_criteria>
- sandboxStrategy entered in project creation wizard persists to database (not silently discarded)
- sandboxStrategy displayed on project settings page reflects stored value
- sandboxStrategy editable on settings page and update persists
- Questions can be created with a category selection
- Question table shows category column
- Category filter in question table filters the list via URL search params
- TypeScript compiles with zero errors
- Database schema is in sync
</success_criteria>

<output>
After completion, create `.planning/phases/06-schema-fixes/06-01-SUMMARY.md`
</output>
