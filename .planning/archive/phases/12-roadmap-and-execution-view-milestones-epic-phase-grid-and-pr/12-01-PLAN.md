---
phase: 12-roadmap-and-execution-view-milestones-epic-phase-grid-and-pr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/sidebar.tsx
  - src/app/(dashboard)/projects/[projectId]/roadmap/page.tsx
  - src/app/(dashboard)/projects/[projectId]/roadmap/roadmap-client.tsx
  - src/components/roadmap/milestones-tab.tsx
  - src/components/roadmap/milestone-form-sheet.tsx
  - src/components/roadmap/milestone-link-stories.tsx
  - src/components/roadmap/milestone-progress-bar.tsx
  - src/actions/milestones.ts
  - src/lib/milestone-progress.ts
  - tests/lib/milestone-progress.test.ts
  - tests/actions/milestones.test.ts
autonomous: true
requirements:
  - PRD-17.2-MILESTONES
user_setup: []

must_haves:
  truths:
    - "User can navigate to /projects/[id]/roadmap from the sidebar"
    - "User can create a milestone with name, description, target date, status, and sort order"
    - "User can edit and delete milestones"
    - "User can link stories to a milestone via multi-select dialog"
    - "Milestone progress bar shows computed percentage with color coding"
    - "Milestones tab shows a table sorted by sortOrder with status badges and progress bars"
  artifacts:
    - path: "src/components/layout/sidebar.tsx"
      provides: "Roadmap nav item with phase: 12, CURRENT_PHASE updated"
      contains: "label: \"Roadmap\""
    - path: "src/app/(dashboard)/projects/[projectId]/roadmap/page.tsx"
      provides: "Server component loading milestones, epics, stories"
      exports: ["default"]
    - path: "src/app/(dashboard)/projects/[projectId]/roadmap/roadmap-client.tsx"
      provides: "Client component with tabbed sub-views"
      contains: "useQueryState"
    - path: "src/actions/milestones.ts"
      provides: "Milestone CRUD + story linking server actions"
      exports: ["createMilestone", "updateMilestone", "deleteMilestone", "linkStoriesToMilestone", "unlinkStoryFromMilestone"]
    - path: "src/lib/milestone-progress.ts"
      provides: "computeMilestoneProgress pure function"
      exports: ["computeMilestoneProgress"]
    - path: "tests/lib/milestone-progress.test.ts"
      provides: "Unit tests for progress computation"
      min_lines: 30
  key_links:
    - from: "src/components/layout/sidebar.tsx"
      to: "/projects/[id]/roadmap"
      via: "NavItem href"
      pattern: "roadmap"
    - from: "src/components/roadmap/milestones-tab.tsx"
      to: "src/actions/milestones.ts"
      via: "server action imports"
      pattern: "import.*milestones"
    - from: "src/app/(dashboard)/projects/[projectId]/roadmap/page.tsx"
      to: "src/lib/project-scope.ts"
      via: "scopedPrisma for project isolation"
      pattern: "scopedPrisma"
---

<objective>
Build the Roadmap page routing, sidebar navigation, milestone CRUD with server actions, milestone progress computation, story linking, and the Milestones tab UI.

Purpose: Delivers the core milestone management experience -- the primary tab of the Roadmap view. Users can create milestones to track project delivery goals, link stories to them, and see computed progress.

Output: Working /projects/[id]/roadmap route with Milestones tab showing table view, Sheet create/edit forms, story linking dialog, and color-coded progress bars. Also creates the tabbed shell used by Plans 02-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-roadmap-and-execution-view-milestones-epic-phase-grid-and-pr/12-CONTEXT.md
@.planning/phases/12-roadmap-and-execution-view-milestones-epic-phase-grid-and-pr/12-RESEARCH.md
@.planning/phases/12-roadmap-and-execution-view-milestones-epic-phase-grid-and-pr/12-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From prisma/schema.prisma:
```prisma
model Milestone {
  id          String          @id @default(cuid())
  projectId   String
  name        String
  description String?
  targetDate  DateTime?
  status      MilestoneStatus @default(NOT_STARTED)
  sortOrder   Int
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt
  project     Project         @relation(fields: [projectId], references: [id], onDelete: Cascade)
  milestoneStories MilestoneStory[]
}

model MilestoneStory {
  id          String    @id @default(cuid())
  milestoneId String
  storyId     String
  milestone   Milestone @relation(fields: [milestoneId], references: [id], onDelete: Cascade)
  story       Story     @relation(fields: [storyId], references: [id], onDelete: Cascade)
  @@unique([milestoneId, storyId])
}

enum MilestoneStatus { NOT_STARTED IN_PROGRESS COMPLETE }
```

From src/lib/safe-action.ts:
```typescript
export const actionClient = createSafeActionClient({...}).use(async ({ next }) => {
  const { userId } = await auth()
  if (!userId) throw new Error("Unauthorized")
  return next({ ctx: { userId } })
})
```

From src/lib/project-scope.ts:
- "Milestone" is already in the SCOPED_ENTITIES list
- scopedPrisma(projectId) returns a Prisma client scoped to the project

From src/components/layout/sidebar.tsx:
```typescript
interface NavItem { label: string; icon: LucideIcon; href: string; phase?: number; roles?: string[]; badge?: number; disabled?: boolean }
const CURRENT_PHASE = 1  // Must be updated to 12
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Milestone progress computation, server actions, and unit tests</name>
  <files>src/lib/milestone-progress.ts, src/actions/milestones.ts, tests/lib/milestone-progress.test.ts, tests/actions/milestones.test.ts</files>
  <read_first>
    - src/actions/stories.ts (server action pattern: Zod schemas, actionClient, getCurrentMember, scopedPrisma, revalidatePath)
    - src/lib/safe-action.ts (actionClient definition)
    - src/lib/project-scope.ts (scopedPrisma pattern, confirm Milestone in scoped entities)
    - src/lib/auth.ts (getCurrentMember signature)
    - prisma/schema.prisma (Milestone, MilestoneStory, Story models + enums)
    - tests/actions/stories.test.ts (test pattern for server actions)
    - tests/lib/ (existing lib test patterns)
  </read_first>
  <behavior>
    - computeMilestoneProgress({milestoneStories: []}) returns {percentage: 0, completedStories: 0, totalStories: 0, unresolvedBlockingQuestions: 0}
    - computeMilestoneProgress with 2 DONE stories out of 4 total, 0 blocking questions returns {percentage: 50, completedStories: 2, totalStories: 4, unresolvedBlockingQuestions: 0}
    - computeMilestoneProgress with 3 DONE out of 4, 1 unresolved blocking question returns percentage less than 75 (blocking penalty applied)
    - computeMilestoneProgress with all stories DONE returns {percentage: 100, ...}
    - createMilestone with valid input creates a milestone and revalidates /projects/[id]/roadmap
    - updateMilestone with valid input updates the milestone
    - deleteMilestone cascades MilestoneStory deletes
    - linkStoriesToMilestone creates MilestoneStory records for each storyId
    - unlinkStoryFromMilestone deletes the MilestoneStory record
  </behavior>
  <action>
    **1. Create `src/lib/milestone-progress.ts`** (per D-15, D-16):

    Export a pure function `computeMilestoneProgress` that accepts a milestone object with nested `milestoneStories[].story` (including `status: string` and `questionBlocksStories[].question.status`).

    Formula per PRD 17.2:
    - `storyProgress = (storiesWithStatus("DONE").length / totalStories) * 100`
    - `unresolvedBlockingQuestions` = count of questions in questionBlocksStories where status is NOT "ANSWERED" and NOT "REVIEWED"
    - `blockingPenalty = unresolvedBlockingQuestions > 0 ? Math.min(storyProgress * 0.25, unresolvedBlockingQuestions * 5) : 0`
    - `percentage = Math.max(0, Math.round(storyProgress - blockingPenalty))`
    - Return `{ percentage, completedStories, totalStories, unresolvedBlockingQuestions }`
    - Guard: if totalStories === 0, return all zeros

    Export a helper `getProgressColor(percentage: number): "green" | "yellow" | "red"`:
    - percentage > 75 -> "green"
    - percentage >= 25 -> "yellow"
    - percentage < 25 -> "red"

    **2. Create `src/actions/milestones.ts`** following the exact pattern from `src/actions/stories.ts`:

    Add `"use server"` directive at top.

    Import: `z` from "zod", `revalidatePath` from "next/cache", `actionClient` from "@/lib/safe-action", `prisma` from "@/lib/db", `scopedPrisma` from "@/lib/project-scope", `getCurrentMember` from "@/lib/auth", `createId` from "@paralleldrive/cuid2", `MilestoneStatus` from "@/generated/prisma".

    Define Zod schemas:
    - `createMilestoneSchema`: projectId (string), name (string, min 1, max 200), description (string, optional), targetDate (string, optional -- ISO date string), status (z.nativeEnum(MilestoneStatus), optional, default NOT_STARTED), sortOrder (number, int)
    - `updateMilestoneSchema`: projectId (string), milestoneId (string), name (string, min 1, max 200, optional), description (string, optional, nullable), targetDate (string, optional, nullable), status (z.nativeEnum(MilestoneStatus), optional), sortOrder (number, int, optional)
    - `deleteMilestoneSchema`: projectId (string), milestoneId (string)
    - `linkStoriesSchema`: projectId (string), milestoneId (string), storyIds (z.array(z.string()).min(1))
    - `unlinkStorySchema`: projectId (string), milestoneId (string), storyId (string)

    Implement 5 server actions with `actionClient.schema(schema).action(handler)`:
    - `createMilestone`: getCurrentMember(projectId) for auth, scopedPrisma(projectId), create milestone with createId() as id, parse targetDate to Date if provided. revalidatePath(`/projects/${projectId}/roadmap`).
    - `updateMilestone`: getCurrentMember, scopedPrisma, findFirst to verify milestone exists in project, update fields. revalidatePath.
    - `deleteMilestone`: getCurrentMember, scopedPrisma, findFirst to verify, delete (MilestoneStory cascade handled by Prisma FK onDelete). revalidatePath.
    - `linkStoriesToMilestone`: getCurrentMember, verify milestone belongs to project, createMany MilestoneStory records with skipDuplicates. revalidatePath.
    - `unlinkStoryFromMilestone`: getCurrentMember, delete MilestoneStory where milestoneId+storyId. revalidatePath.

    **3. Create test files:**

    `tests/lib/milestone-progress.test.ts`:
    - Test computeMilestoneProgress with 0 stories -> all zeros
    - Test with mixed statuses, no blocking questions
    - Test with blocking questions reducing percentage
    - Test with all DONE stories -> 100%
    - Test getProgressColor thresholds: 80->green, 50->yellow, 10->red, 75->yellow (boundary), 25->yellow (boundary), 24->red

    `tests/actions/milestones.test.ts`:
    - Follow the mock pattern from existing tests/actions/ files
    - Test createMilestone creates record
    - Test deleteMilestone removes record
    - Test linkStoriesToMilestone creates join records
  </action>
  <verify>
    <automated>cd /Users/michaelrihm/Documents/Salesforce-Consulting-SDLC-App && npx vitest run tests/lib/milestone-progress.test.ts tests/actions/milestones.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/milestone-progress.ts contains `export function computeMilestoneProgress(`
    - src/lib/milestone-progress.ts contains `export function getProgressColor(`
    - src/lib/milestone-progress.ts contains `if (total === 0)` guard
    - src/actions/milestones.ts starts with `"use server"`
    - src/actions/milestones.ts contains `export const createMilestone = actionClient`
    - src/actions/milestones.ts contains `export const updateMilestone = actionClient`
    - src/actions/milestones.ts contains `export const deleteMilestone = actionClient`
    - src/actions/milestones.ts contains `export const linkStoriesToMilestone = actionClient`
    - src/actions/milestones.ts contains `export const unlinkStoryFromMilestone = actionClient`
    - src/actions/milestones.ts contains `getCurrentMember(` in every action handler
    - src/actions/milestones.ts contains `revalidatePath(` in every action handler
    - tests/lib/milestone-progress.test.ts exits 0
    - tests/actions/milestones.test.ts exits 0
  </acceptance_criteria>
  <done>All 5 server actions implemented with auth + project scoping. Progress computation is pure, tested, and handles edge cases (0 stories, blocking questions). All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Roadmap page route, sidebar nav, tabbed shell, and Milestones tab UI</name>
  <files>src/components/layout/sidebar.tsx, src/app/(dashboard)/projects/[projectId]/roadmap/page.tsx, src/app/(dashboard)/projects/[projectId]/roadmap/roadmap-client.tsx, src/components/roadmap/milestones-tab.tsx, src/components/roadmap/milestone-form-sheet.tsx, src/components/roadmap/milestone-link-stories.tsx, src/components/roadmap/milestone-progress-bar.tsx</files>
  <read_first>
    - src/components/layout/sidebar.tsx (NavItem pattern, CURRENT_PHASE constant, existing items)
    - src/app/(dashboard)/projects/[projectId]/work/page.tsx (server component pattern with scopedPrisma + JSON.parse(JSON.stringify()))
    - src/app/(dashboard)/projects/[projectId]/work/work-page-client.tsx (client component pattern)
    - src/app/(dashboard)/projects/[projectId]/stories/[storyId]/story-detail-client.tsx (Tabs pattern)
    - src/components/shared/empty-state.tsx (EmptyState component interface)
    - src/actions/milestones.ts (just created in Task 1 -- action imports)
    - src/lib/milestone-progress.ts (just created in Task 1 -- progress function)
    - .planning/phases/12-roadmap-and-execution-view-milestones-epic-phase-grid-and-pr/12-UI-SPEC.md (visual contract)
  </read_first>
  <action>
    **1. Update `src/components/layout/sidebar.tsx`** (per D-01, D-03, Pitfall 6):

    - Change `const CURRENT_PHASE = 1` to `const CURRENT_PHASE = 12`
    - Add import for `Map` icon from lucide-react (add to existing import line)
    - Add Roadmap NavItem BEFORE the "Settings" item in `buildNavItems()`:
      ```
      {
        label: "Roadmap",
        icon: Map,
        href: activeProjectId ? `/projects/${activeProjectId}/roadmap` : "#",
        phase: 12,
        disabled: !activeProjectId,
      }
      ```
    - No `roles` property (visible to all per D-03)

    **2. Create `src/app/(dashboard)/projects/[projectId]/roadmap/page.tsx`** (server component):

    Follow exact pattern from work/page.tsx:
    - Import scopedPrisma, getCurrentMember, Suspense, Skeleton
    - Import RoadmapClient from "./roadmap-client"
    - Import prisma from "@/lib/db" (for EpicPhase queries -- not in scopedPrisma)
    - Accept `{ params: Promise<{ projectId: string }> }`
    - `await getCurrentMember(projectId)` for auth
    - `const db = scopedPrisma(projectId)`
    - Load in parallel with Promise.all:
      - `milestones`: db.milestone.findMany with include milestoneStories -> story (include status, displayId, title, epicId, questionBlocksStories -> question {status}), orderBy sortOrder asc
      - `epics`: db.epic.findMany with include _count { features, stories }, orderBy sortOrder asc
      - `allStories`: db.story.findMany with select id, displayId, title, epicId, status -- for story linking dialog
    - Extract epicIds from epics result
    - Query epicPhases: `prisma.epicPhase.findMany({ where: { epicId: { in: epicIds } } })` (NOT scopedPrisma -- EpicPhase has no projectId, per Pitfall 1)
    - Serialize all data with `JSON.parse(JSON.stringify(data))`
    - Render `<Suspense fallback={<RoadmapPageSkeleton />}><RoadmapClient projectId={projectId} milestones={...} epics={...} epicPhases={...} allStories={...} /></Suspense>`
    - Create RoadmapPageSkeleton with 3 skeleton rows per UI-SPEC loading states

    **3. Create `src/app/(dashboard)/projects/[projectId]/roadmap/roadmap-client.tsx`** (per D-02):

    - `"use client"` directive
    - Import useQueryState from "nuqs" for tab persistence
    - Import Tabs, TabsList, TabsTrigger, TabsContent from "@/components/ui/tabs"
    - Import MilestonesTab from "@/components/roadmap/milestones-tab"
    - Props: projectId, milestones (serialized), epics (serialized), epicPhases (serialized), allStories (serialized)
    - `const [tab, setTab] = useQueryState("tab", { defaultValue: "milestones" })`
    - Render page title "Roadmap" (28px semibold per UI-SPEC Display typography)
    - Render Tabs with value={tab} onValueChange={setTab}:
      - TabsTrigger value="milestones" label "Milestones"
      - TabsTrigger value="epic-phases" label "Epic Phases"
      - TabsTrigger value="execution-plan" label "Execution Plan"
    - TabsContent "milestones": render `<MilestonesTab projectId={projectId} milestones={milestones} allStories={allStories} />`
    - TabsContent "epic-phases": render `<div>Epic Phase Grid placeholder</div>` (Plan 02 replaces)
    - TabsContent "execution-plan": render `<div>Execution Plan placeholder</div>` (Plan 03 replaces)

    **4. Create `src/components/roadmap/milestones-tab.tsx`** (per D-04):

    - `"use client"` directive
    - Import Table components, Badge, Button, EmptyState
    - Import MilestoneFormSheet, MilestoneLinkStories, MilestoneProgressBar
    - Import deleteMilestone from "@/actions/milestones"
    - Import AlertDialog components for delete confirmation
    - Import { toast } from "sonner"
    - Import { format } from "date-fns"
    - Props: projectId, milestones array, allStories array
    - State: selectedMilestone (for edit), showForm (boolean), deletingId (string|null), linkingMilestoneId (string|null)
    - If milestones.length === 0: render EmptyState with heading "No milestones yet", body "Create milestones to track progress toward project delivery goals.", action Button "Create First Milestone" that opens the Sheet
    - Otherwise render:
      - Header row with "Milestones" heading and "New Milestone" button (accent color per UI-SPEC)
      - Table with columns: Name, Target Date, Status (Badge), Progress (MilestoneProgressBar), Stories (count), Actions (edit, link stories, delete icons)
      - Target date column: `format(new Date(targetDate), "MMM d, yyyy")` or "TBD" if null
      - Status badges: NOT_STARTED (gray bg), IN_PROGRESS (blue bg), COMPLETE (green bg) per UI-SPEC semantic colors
      - Actions column: edit pencil icon (opens Sheet), link icon (opens link dialog), trash icon (opens AlertDialog)
      - Each icon-only button has `aria-label="Edit {name}"`, `aria-label="Delete {name}"` per UI-SPEC accessibility
    - Delete handler: call deleteMilestone action, toast success/error per UI-SPEC error states
    - Render MilestoneFormSheet and MilestoneLinkStories as controlled components

    **5. Create `src/components/roadmap/milestone-form-sheet.tsx`** (per D-05):

    - `"use client"` directive
    - Import Sheet, SheetContent, SheetHeader, SheetTitle from "@/components/ui/sheet"
    - Import Input, Textarea, Label, Select, Button from shadcn
    - Import react-hook-form useForm + zodResolver
    - Import createMilestone, updateMilestone from "@/actions/milestones"
    - Import { toast } from "sonner"
    - Props: open, onOpenChange, projectId, milestone (optional -- null for create, populated for edit)
    - Form fields per D-05: name (Input, required), description (Textarea), targetDate (Input type="date", optional), status (Select: NOT_STARTED, IN_PROGRESS, COMPLETE), sortOrder (Input type="number")
    - On submit: call createMilestone or updateMilestone server action based on whether milestone prop exists
    - On success: toast("Milestone saved"), onOpenChange(false)
    - On error: toast.error("Failed to save milestone. Please try again.") per UI-SPEC error copy

    **6. Create `src/components/roadmap/milestone-link-stories.tsx`** (per D-06):

    - `"use client"` directive
    - Import Dialog, Checkbox, Button, Collapsible
    - Import linkStoriesToMilestone from "@/actions/milestones"
    - Props: open, onOpenChange, projectId, milestoneId, milestoneName, allStories, linkedStoryIds
    - Filter out already-linked stories
    - Group remaining stories by epicId
    - Render Dialog with title "Link Stories to {milestoneName}" per UI-SPEC copy
    - For each epic group: Collapsible section with epic name header, checkbox per story showing displayId + title
    - Track selected story IDs in local state (Set<string>)
    - "Link Selected Stories" button calls linkStoriesToMilestone
    - If all stories linked: show "All stories are linked" per UI-SPEC empty state
    - On success: toast, close dialog. On error: toast.error, keep dialog open per UI-SPEC error states

    **7. Create `src/components/roadmap/milestone-progress-bar.tsx`** (per D-16):

    - Import Progress from "@/components/ui/progress"
    - Import computeMilestoneProgress, getProgressColor from "@/lib/milestone-progress"
    - Props: milestone (with milestoneStories nested)
    - Compute progress using computeMilestoneProgress
    - If totalStories === 0: render "No stories linked" text (muted, 12px) per UI-SPEC copy
    - Otherwise: render Progress component with value={percentage}, custom color class based on getProgressColor:
      - green: `[&>div]:bg-[hsl(160,60%,45%)]`
      - yellow: `[&>div]:bg-[hsl(30,80%,55%)]`
      - red: `[&>div]:bg-[hsl(0,84%,60%)]`
    - Render percentage label next to bar: `{percentage}%` and `{completedStories}/{totalStories} stories`
    - If unresolvedBlockingQuestions > 0: render small text "Blocked: {N} open question(s)" per UI-SPEC copy
  </action>
  <verify>
    <automated>cd /Users/michaelrihm/Documents/Salesforce-Consulting-SDLC-App && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/sidebar.tsx contains `const CURRENT_PHASE = 12`
    - src/components/layout/sidebar.tsx contains `label: "Roadmap"`
    - src/components/layout/sidebar.tsx contains `phase: 12`
    - src/app/(dashboard)/projects/[projectId]/roadmap/page.tsx contains `scopedPrisma(projectId)`
    - src/app/(dashboard)/projects/[projectId]/roadmap/page.tsx contains `getCurrentMember(projectId)`
    - src/app/(dashboard)/projects/[projectId]/roadmap/page.tsx contains `JSON.parse(JSON.stringify(`
    - src/app/(dashboard)/projects/[projectId]/roadmap/page.tsx contains `prisma.epicPhase.findMany`
    - src/app/(dashboard)/projects/[projectId]/roadmap/roadmap-client.tsx contains `"use client"`
    - src/app/(dashboard)/projects/[projectId]/roadmap/roadmap-client.tsx contains `useQueryState("tab"`
    - src/components/roadmap/milestones-tab.tsx contains `"No milestones yet"`
    - src/components/roadmap/milestones-tab.tsx contains `aria-label=`
    - src/components/roadmap/milestone-form-sheet.tsx contains `SheetContent`
    - src/components/roadmap/milestone-link-stories.tsx contains `linkStoriesToMilestone`
    - src/components/roadmap/milestone-progress-bar.tsx contains `computeMilestoneProgress`
    - src/components/roadmap/milestone-progress-bar.tsx contains `No stories linked`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Roadmap route accessible from sidebar, Milestones tab fully functional with create/edit/delete/link-stories and computed progress bars. Tabbed shell ready for Plans 02 and 03 to fill Epic Phases and Execution Plan tabs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server actions | Untrusted input from client forms passed to milestone CRUD actions |
| server -> database | Milestone queries must be project-scoped to prevent cross-project access |
| EpicPhase indirect scoping | EpicPhase has no projectId -- must verify epic ownership before any EpicPhase operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | milestones.ts | mitigate | getCurrentMember(projectId) auth check in every server action; scopedPrisma for DB queries |
| T-12-02 | Tampering | milestones.ts | mitigate | Zod schema validation on all inputs via next-safe-action |
| T-12-03 | Information Disclosure | page.tsx | mitigate | scopedPrisma ensures milestones only returned for active project; getCurrentMember blocks unauthenticated access |
| T-12-04 | Elevation of Privilege | milestones.ts deleteMilestone | mitigate | getCurrentMember verifies user is project member before delete; findFirst confirms milestone belongs to project |
| T-12-05 | Tampering | milestone-link-stories | mitigate | linkStoriesToMilestone verifies milestone belongs to project before creating MilestoneStory records |
</threat_model>

<verification>
1. Navigate to sidebar -- "Roadmap" item visible for all roles
2. Click Roadmap -- page loads with Milestones tab active
3. Create milestone via Sheet form -- appears in table
4. Edit milestone -- values update
5. Link stories to milestone -- progress bar updates
6. Delete milestone -- removed from table, confirmation dialog shown
7. Tab switching works and persists in URL (?tab=epic-phases)
8. Empty state shown when no milestones exist
9. TypeScript compilation clean
10. All unit tests pass
</verification>

<success_criteria>
- Roadmap page accessible at /projects/[id]/roadmap with sidebar navigation
- Milestones tab shows table with CRUD operations
- Progress bars computed from linked story statuses with color coding
- Story linking dialog groups stories by epic
- All server actions have auth + project scoping
- Unit tests for progress computation and server actions pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-roadmap-and-execution-view-milestones-epic-phase-grid-and-pr/12-01-SUMMARY.md`
</output>
