---
phase: 05-document-generation-qa-and-administration
plan: 08
type: execute
wave: 3
depends_on: ["05-03", "05-07"]
files_modified:
  - src/components/jira/jira-config-form.tsx
  - src/components/jira/sync-status-badge.tsx
  - src/app/(dashboard)/projects/[projectId]/settings/page.tsx
  - src/components/work/story-table.tsx
autonomous: true
requirements:
  - ADMIN-01
  - PROJ-04
  - PROJ-05

must_haves:
  truths:
    - "Schema pushed with JiraConfig and JiraSyncRecord tables"
    - "PM can configure Jira instance URL and API token in project settings"
    - "Sync status badge shows Synced/Pending/Failed per story in story table per D-17"
    - "PM can archive a project from settings page"
    - "PM can reactivate an archived project from settings page"
    - "Bulk retry available for failed syncs"
  artifacts:
    - path: "src/components/jira/jira-config-form.tsx"
      provides: "Jira configuration form for settings page"
    - path: "src/components/jira/sync-status-badge.tsx"
      provides: "Sync status badge component"
    - path: "src/app/(dashboard)/projects/[projectId]/settings/page.tsx"
      provides: "Updated settings page with Jira and archival sections"
  key_links:
    - from: "src/components/jira/jira-config-form.tsx"
      to: "src/actions/jira-sync.ts"
      via: "calls saveJiraConfig, getJiraConfig, toggleJiraSync"
      pattern: "saveJiraConfig|getJiraConfig|toggleJiraSync"
    - from: "src/components/jira/sync-status-badge.tsx"
      to: "src/components/work/story-table.tsx"
      via: "rendered in story table Jira column per D-17"
      pattern: "SyncStatusBadge"
    - from: "src/app/(dashboard)/projects/[projectId]/settings/page.tsx"
      to: "src/actions/project-archive.ts"
      via: "calls archiveProject and reactivateProject"
      pattern: "archiveProject|reactivateProject"
---

<objective>
Push the JiraConfig/JiraSyncRecord schema, create settings UI for Jira configuration and project archival, and wire SyncStatusBadge into the story table.

Purpose: Complete the Jira integration UI and project lifecycle management. Push schema changes safely as a separate step, then build the settings UI and wire the sync status badge into story views per D-17.
Output: Schema pushed, settings page with Jira config + archive/reactivate, sync badge in story table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-document-generation-qa-and-administration/05-CONTEXT.md
@.planning/phases/05-document-generation-qa-and-administration/05-UI-SPEC.md
@.planning/phases/05-document-generation-qa-and-administration/05-07-SUMMARY.md

<interfaces>
From src/actions/jira-sync.ts (created in Plan 07):
```typescript
export const saveJiraConfig = actionClient.schema(...).action(...)
export const getJiraConfig = actionClient.schema(...).action(...)
export const toggleJiraSync = actionClient.schema(...).action(...)
export const getJiraSyncStatus = actionClient.schema(...).action(...)
export const retryFailedSyncs = actionClient.schema(...).action(...)
```

From src/actions/project-archive.ts (created in Plan 07):
```typescript
export const archiveProject = actionClient.schema(...).action(...)
export const reactivateProject = actionClient.schema(...).action(...)
```

From src/components/work/story-table.tsx (updated in Plan 03 to add defect count):
```typescript
export interface StoryRow { id: string; displayId: string; title: string; status: StoryStatus; defectCount?: number; ... }
export function StoryTable({ stories, onEdit, projectId, epicId, featureId, ... }: StoryTableProps) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Schema push and create Jira config form and sync status badge</name>
  <files>src/components/jira/jira-config-form.tsx, src/components/jira/sync-status-badge.tsx</files>
  <read_first>
    - prisma/schema.prisma (verify JiraConfig and JiraSyncRecord models from Plan 07)
    - src/components/ui/switch.tsx (shadcn Switch for Jira toggle)
    - src/components/ui/badge.tsx (for sync status badges)
    - .planning/phases/05-document-generation-qa-and-administration/05-UI-SPEC.md (lines 540-600+ for Jira config and sync badge layouts)
  </read_first>
  <action>
1. **[BLOCKING] Run schema push:**
   ```bash
   npx prisma db push
   ```
   This adds JiraConfig and JiraSyncRecord tables. If destructive changes are detected, use `npx prisma db push --accept-data-loss` (development database only).
   Then regenerate Prisma client:
   ```bash
   npx prisma generate
   ```

2. Create `src/components/jira/jira-config-form.tsx`:
   - Client component
   - Props: `{ projectId: string; existingConfig?: JiraConfig | null }`
   - Form with fields per UI-SPEC Jira configuration layout:
     - Enable Jira Sync: Switch toggle (calls `toggleJiraSync`)
     - Jira Instance URL: input (placeholder "https://your-company.atlassian.net")
     - Email: input (Jira account email)
     - API Token: password input (shows "****" if existing config, empty for new)
     - Jira Project Key: input (e.g., "PROJ")
   - "Save Configuration" accent button, "Disconnect" destructive outline button (if configured)
   - Uses react-hook-form with Zod resolver
   - On submit: calls `saveJiraConfig` action
   - Success toast: "Jira configuration saved"
   - Disconnect triggers AlertDialog: "Disconnect Jira?" per UI-SPEC destructive actions

3. Create `src/components/jira/sync-status-badge.tsx`:
   - Client component
   - Props: `{ status: "SYNCED" | "PENDING" | "FAILED"; lastSyncAt?: Date | null; jiraIssueKey?: string | null }`
   - Badge colors per UI-SPEC:
     - SYNCED: outline badge with `border-primary text-primary` (accent border), shows jiraIssueKey if available
     - PENDING: outline badge with `text-muted-foreground`
     - FAILED: solid badge with `bg-destructive text-white`
   - Tooltip showing lastSyncAt timestamp if available
   - If no status prop provided (story not synced), render nothing
  </action>
  <verify>
    <automated>npx prisma db push --accept-data-loss 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/jira/jira-config-form.tsx contains `"use client"`
    - src/components/jira/jira-config-form.tsx contains `saveJiraConfig`
    - src/components/jira/jira-config-form.tsx contains `Switch`
    - src/components/jira/jira-config-form.tsx contains `useForm`
    - src/components/jira/sync-status-badge.tsx contains `SYNCED`
    - src/components/jira/sync-status-badge.tsx contains `PENDING`
    - src/components/jira/sync-status-badge.tsx contains `FAILED`
    - src/components/jira/sync-status-badge.tsx contains `jiraIssueKey`
  </acceptance_criteria>
  <done>Schema pushed with JiraConfig and JiraSyncRecord tables. Jira config form with toggle, URL, email, token, and project key fields. Sync status badge with semantic colors and Jira issue key display.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SyncStatusBadge into story table and update settings page</name>
  <files>src/components/work/story-table.tsx, src/app/(dashboard)/projects/[projectId]/settings/page.tsx</files>
  <read_first>
    - src/components/work/story-table.tsx (current state after Plan 03 changes)
    - src/app/(dashboard)/projects/[projectId]/settings/page.tsx (existing settings page)
    - src/components/ui/alert-dialog.tsx (for archive/reactivate confirmations)
    - .planning/phases/05-document-generation-qa-and-administration/05-UI-SPEC.md (lines 540-600+ for settings page layout)
  </read_first>
  <action>
1. Update `src/components/work/story-table.tsx` to wire SyncStatusBadge per D-17:
   - Import `SyncStatusBadge` from `@/components/jira/sync-status-badge`
   - Add `jiraSyncStatus?: { status: string; lastSyncAt: Date | null; jiraIssueKey: string | null }` to StoryRow interface
   - Add `hasJiraConfig?: boolean` to StoryTableProps (to conditionally show column)
   - When `hasJiraConfig` is true, add a "Jira" column after the Status column showing `<SyncStatusBadge status={row.jiraSyncStatus?.status} lastSyncAt={row.jiraSyncStatus?.lastSyncAt} jiraIssueKey={row.jiraSyncStatus?.jiraIssueKey} />`
   - When `hasJiraConfig` is false, omit the Jira column entirely

2. Update `src/app/(dashboard)/projects/[projectId]/settings/page.tsx`:
   - Add two new sections below existing settings content:

   **Jira Integration section:**
   - Section heading: "Jira Integration" (18px/600)
   - Separator
   - Fetch existing config via `getJiraConfig({ projectId })`
   - JiraConfigForm component with existingConfig prop
   - If configured and has failed syncs, show "Retry Failed Syncs" button that calls `retryFailedSyncs({ projectId })`
   - Empty state if not configured: "Jira sync not configured" heading, "Optionally connect a client Jira instance to push stories and status updates." body

   **Project Lifecycle section:**
   - Section heading: "Project Lifecycle" (18px/600)
   - Separator
   - If project is ACTIVE:
     - "Archive Project" destructive outline button
     - Description: "Archive this project to make it read-only. All data will be retained."
     - Click triggers AlertDialog per UI-SPEC: "Archive this project?" with cancel and "Archive Project" destructive button
     - On confirm: calls `archiveProject({ projectId })`, shows success toast, redirects to projects list
   - If project is ARCHIVED:
     - Archived badge with Lock icon
     - "Reactivate Project" accent button
     - Description: "Reactivate to resume making changes to this project."
     - Click triggers AlertDialog per UI-SPEC: "Reactivate this project?" with cancel and "Reactivate" accent button
     - On confirm: calls `reactivateProject({ projectId })`, shows success toast, page refreshes
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/work/story-table.tsx contains `SyncStatusBadge`
    - src/components/work/story-table.tsx contains `jiraSyncStatus`
    - src/components/work/story-table.tsx contains `hasJiraConfig`
    - src/app/(dashboard)/projects/[projectId]/settings/page.tsx contains `Jira Integration`
    - src/app/(dashboard)/projects/[projectId]/settings/page.tsx contains `Project Lifecycle`
    - src/app/(dashboard)/projects/[projectId]/settings/page.tsx contains `Archive Project`
    - src/app/(dashboard)/projects/[projectId]/settings/page.tsx contains `Reactivate`
    - src/app/(dashboard)/projects/[projectId]/settings/page.tsx contains `AlertDialog`
    - src/app/(dashboard)/projects/[projectId]/settings/page.tsx contains `getJiraConfig`
    - src/app/(dashboard)/projects/[projectId]/settings/page.tsx contains `retryFailedSyncs`
  </acceptance_criteria>
  <done>SyncStatusBadge wired into story table (Jira column shown only when project has Jira config, per D-17). Settings page shows Jira configuration form with retry-failed option, and project lifecycle section with archive/reactivate dialogs. TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Jira API token | Sensitive credential encrypted at rest, masked in UI |
| PM -> archive action | Role-gated destructive operation with confirmation dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | I (Info Disclosure) | jira-config-form | mitigate | API token shown as **** in UI. Never returned in plain text. Password input type. |
| T-05-21 | E (Elevation) | settings page | mitigate | Archive/reactivate restricted to PM role via server action. AlertDialog prevents accidental clicks. |
</threat_model>

<verification>
- `npx prisma db push` succeeds
- `npx tsc --noEmit` passes
- Settings page renders Jira config and archive sections
- Story table shows Jira column when config exists
- Sync status badge renders correct colors
</verification>

<success_criteria>
Schema pushed safely. Settings page provides Jira configuration with encrypted token storage, sync toggle, disconnect, and failed sync retry. Project lifecycle section enables archive/reactivate with confirmation dialogs. SyncStatusBadge visible per story in story table when Jira is configured (per D-17).
</success_criteria>

<output>
After completion, create `.planning/phases/05-document-generation-qa-and-administration/05-08-SUMMARY.md`
</output>
