---
phase: 05-document-generation-qa-and-administration
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/unit/defect-status-machine.test.ts
  - tests/unit/archive-guard.test.ts
  - tests/unit/s3-storage.test.ts
  - tests/unit/branding.test.ts
  - tests/unit/document-renderers.test.ts
  - tests/unit/document-templates.test.ts
  - tests/unit/jira-field-mapping.test.ts
  - tests/unit/jira-sync.test.ts
  - tests/unit/test-executions-actions.test.ts
  - tests/unit/defects-actions.test.ts
  - tests/unit/documents-actions.test.ts
  - tests/unit/pm-dashboard-synthesis.test.ts
  - tests/helpers/mock-s3.ts
  - tests/helpers/mock-jira.ts
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - QA-01
  - QA-02
  - QA-03
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - PROJ-04
  - PROJ-05

must_haves:
  truths:
    - "All 12 test files exist with failing test stubs"
    - "Mock helpers for S3 and Jira API are available"
    - "Each test file describes expected behavior for its module"
    - "Running vitest produces RED results for all stubs"
  artifacts:
    - path: "tests/unit/defect-status-machine.test.ts"
      provides: "Tests for defect status transitions and role guards"
    - path: "tests/unit/archive-guard.test.ts"
      provides: "Tests for archive guard middleware"
    - path: "tests/unit/s3-storage.test.ts"
      provides: "Tests for S3 upload and presigned URL generation"
    - path: "tests/unit/document-renderers.test.ts"
      provides: "Tests for DOCX, PPTX, PDF renderers applying branding"
    - path: "tests/unit/jira-field-mapping.test.ts"
      provides: "Tests for story-to-Jira field mapping"
    - path: "tests/unit/jira-sync.test.ts"
      provides: "Tests for Jira push and status sync"
    - path: "tests/unit/test-executions-actions.test.ts"
      provides: "Tests for test case and test execution server actions"
    - path: "tests/unit/defects-actions.test.ts"
      provides: "Tests for defect CRUD and status transition actions"
    - path: "tests/unit/documents-actions.test.ts"
      provides: "Tests for document generation request and listing actions"
    - path: "tests/unit/pm-dashboard-synthesis.test.ts"
      provides: "Tests for PM dashboard data aggregation"
  key_links: []
---

<objective>
Create failing test stubs (TDD RED phase) for all Phase 5 modules: defect status machine, archive guard, S3 storage, document renderers, document templates, Jira field mapping, Jira sync, QA server actions, document server actions, and PM dashboard synthesis.

Purpose: Establish Nyquist-compliant test coverage before implementation begins. Every subsequent plan task verifies against these test files.
Output: 12 test files with failing stubs, 2 mock helpers (S3, Jira).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-document-generation-qa-and-administration/05-CONTEXT.md

<interfaces>
From tests/helpers/mock-prisma.ts (existing mock pattern):
```typescript
// Existing Prisma mock helper -- follow this pattern for S3 and Jira mocks
```

From vitest.config.ts (existing config):
```typescript
// Already configured for tests/unit/** glob
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mock helpers and core logic test stubs</name>
  <files>tests/helpers/mock-s3.ts, tests/helpers/mock-jira.ts, tests/unit/defect-status-machine.test.ts, tests/unit/archive-guard.test.ts, tests/unit/s3-storage.test.ts, tests/unit/branding.test.ts, tests/unit/document-templates.test.ts, tests/unit/jira-field-mapping.test.ts</files>
  <read_first>
    - tests/helpers/mock-prisma.ts (mock helper pattern)
    - vitest.config.ts (test configuration)
    - tests/unit/ (list existing test files for naming pattern)
  </read_first>
  <action>
1. Create `tests/helpers/mock-s3.ts`:
   - Export `createMockS3Client()` returning a mock with `send` method that resolves
   - Export `mockUploadDocument` spy function
   - Export `mockGetDownloadUrl` spy returning a fake presigned URL

2. Create `tests/helpers/mock-jira.ts`:
   - Export `createMockJiraClient()` returning a mock Version3Client with:
     - `issues.createIssue` resolving to `{ id: "10001", key: "PROJ-1" }`
     - `issues.editIssue` resolving
     - `issues.getTransitions` resolving to `{ transitions: [{ id: "1", name: "In Progress" }] }`
     - `issues.doTransition` resolving
   - Export `mockDecrypt` spy returning plaintext

3. Create `tests/unit/defect-status-machine.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"
   // import { canTransitionDefect, getNextDefectStatuses, DEFECT_TRANSITIONS, DEFECT_STATUS_LABELS } from "@/lib/defect-status-machine"

   describe("defect-status-machine", () => {
     describe("DEFECT_TRANSITIONS", () => {
       it.todo("allows OPEN -> ASSIGNED")
       it.todo("disallows OPEN -> FIXED")
       it.todo("allows FIXED -> VERIFIED")
       it.todo("disallows CLOSED -> any status")
     })

     describe("canTransitionDefect", () => {
       it.todo("returns true for valid transition with correct role")
       it.todo("returns false for invalid transition")
       it.todo("requires QA role for VERIFIED transition")
       it.todo("allows any role for non-VERIFIED transitions")
     })

     describe("getNextDefectStatuses", () => {
       it.todo("returns [ASSIGNED] for OPEN status")
       it.todo("returns [] for CLOSED status")
       it.todo("filters by role for VERIFIED-requiring transitions")
     })

     describe("DEFECT_STATUS_LABELS", () => {
       it.todo("maps ASSIGNED to 'In Progress'")
       it.todo("maps all statuses to display labels")
     })
   })
   ```

4. Create `tests/unit/archive-guard.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("archive-guard", () => {
     it.todo("throws error when project status is ARCHIVED")
     it.todo("does not throw when project status is ACTIVE")
     it.todo("throws with specific error message mentioning reactivation")
   })
   ```

5. Create `tests/unit/s3-storage.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("s3-storage", () => {
     describe("uploadDocument", () => {
       it.todo("uploads buffer to S3 with correct key pattern: projects/{projectId}/documents/{type}/{format}/{id}.{ext}")
       it.todo("returns the s3Key after upload")
       it.todo("sets correct content type for DOCX format")
       it.todo("sets correct content type for PDF format")
       it.todo("sets correct content type for PPTX format")
     })

     describe("getDownloadUrl", () => {
       it.todo("generates presigned URL for given s3Key")
       it.todo("uses 300s default TTL")
       it.todo("accepts custom TTL parameter")
     })
   })
   ```

6. Create `tests/unit/branding.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("branding", () => {
     it.todo("exports BRANDING_CONFIG with firmName")
     it.todo("exports BRANDING_CONFIG with headingColor as hex without #")
     it.todo("exports BRANDING_CONFIG with fontFamily")
     it.todo("exports BrandingConfig type")
   })
   ```

7. Create `tests/unit/document-templates.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("document-templates", () => {
     describe("DOCUMENT_TEMPLATES registry", () => {
       it.todo("contains 4 templates")
       it.todo("each template has id, name, description, sections, supportedFormats")
     })

     describe("getTemplate", () => {
       it.todo("returns template by id")
       it.todo("returns undefined for unknown id")
     })

     describe("discovery-report template", () => {
       it.todo("has documentType BRD")
       it.todo("supports DOCX and PDF formats")
       it.todo("has required executive-summary section")
     })

     describe("executive-brief template", () => {
       it.todo("has documentType PRESENTATION")
       it.todo("supports PPTX and PDF formats")
     })
   })
   ```

8. Create `tests/unit/jira-field-mapping.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("jira-field-mapping", () => {
     describe("STATUS_TO_JIRA", () => {
       it.todo("maps DRAFT to 'To Do'")
       it.todo("maps IN_PROGRESS to 'In Progress'")
       it.todo("maps DONE to 'Done'")
       it.todo("maps all StoryStatus values")
     })

     describe("mapStoryToJiraFields", () => {
       it.todo("prefixes summary with displayId")
       it.todo("uses empty string for null description")
       it.todo("includes story points when present")
       it.todo("omits story points when null")
     })
   })
   ```
  </action>
  <verify>
    <automated>npx vitest run tests/unit/defect-status-machine.test.ts tests/unit/archive-guard.test.ts tests/unit/s3-storage.test.ts tests/unit/branding.test.ts tests/unit/document-templates.test.ts tests/unit/jira-field-mapping.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>6 test files created with failing .todo() stubs covering core logic modules. 2 mock helpers created for S3 and Jira. All tests show as pending/todo when run.</done>
</task>

<task type="auto">
  <name>Task 2: Create server action and integration test stubs</name>
  <files>tests/unit/document-renderers.test.ts, tests/unit/jira-sync.test.ts, tests/unit/test-executions-actions.test.ts, tests/unit/defects-actions.test.ts, tests/unit/documents-actions.test.ts, tests/unit/pm-dashboard-synthesis.test.ts</files>
  <read_first>
    - tests/unit/ (list existing test files for patterns)
  </read_first>
  <action>
1. Create `tests/unit/document-renderers.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("document-renderers", () => {
     describe("renderDocx", () => {
       it.todo("returns a Buffer")
       it.todo("applies branding firmName in header")
       it.todo("applies branding headingColor to headings")
       it.todo("renders title page with document title")
       it.todo("renders each section as a heading + body")
     })

     describe("renderPptx", () => {
       it.todo("returns a Buffer")
       it.todo("creates title slide with firmName")
       it.todo("creates content slides per section")
     })

     describe("renderPdf", () => {
       it.todo("returns a Buffer")
       it.todo("includes firm name in header")
       it.todo("includes Confidential in footer")
     })
   })
   ```

2. Create `tests/unit/jira-sync.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("jira-sync", () => {
     describe("pushStoryToJira", () => {
       it.todo("creates new Jira issue when no sync record exists")
       it.todo("updates existing Jira issue when sync record exists")
       it.todo("returns jiraIssueId and jiraIssueKey")
     })

     describe("syncStoryStatus", () => {
       it.todo("transitions Jira issue to matching status")
       it.todo("skips sync when no sync record exists")
       it.todo("logs warning when no matching Jira transition found")
     })
   })
   ```

3. Create `tests/unit/test-executions-actions.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("test-executions actions", () => {
     describe("createTestCase", () => {
       it.todo("creates test case with MANUAL source")
       it.todo("auto-calculates sortOrder")
       it.todo("validates title length 1-500")
     })

     describe("recordTestExecution", () => {
       it.todo("creates execution record with result and notes")
       it.todo("sends TEST_EXECUTION_RECORDED Inngest event")
       it.todo("sends notification to story assignee")
     })

     describe("getStoryTestCases", () => {
       it.todo("returns test cases with latest execution")
       it.todo("orders by sortOrder ascending")
     })
   })
   ```

4. Create `tests/unit/defects-actions.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("defects actions", () => {
     describe("createDefect", () => {
       it.todo("generates DEF-N display ID")
       it.todo("sends DEFECT_CREATED Inngest event")
       it.todo("sends notification to assignee when provided")
     })

     describe("transitionDefectStatus", () => {
       it.todo("validates transition via canTransitionDefect")
       it.todo("rejects invalid transitions")
       it.todo("sends DEFECT_STATUS_CHANGED event")
     })

     describe("getDefects", () => {
       it.todo("filters by status when provided")
       it.todo("filters by severity when provided")
       it.todo("includes story and assignee relations")
     })
   })
   ```

5. Create `tests/unit/documents-actions.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("documents actions", () => {
     describe("requestDocumentGeneration", () => {
       it.todo("validates templateId exists in registry")
       it.todo("validates format is supported by template")
       it.todo("sends DOCUMENT_GENERATION_REQUESTED Inngest event")
       it.todo("restricts to PM/SA roles")
     })

     describe("getDocuments", () => {
       it.todo("returns documents ordered by createdAt desc")
       it.todo("computes version numbers per document type")
       it.todo("filters by documentType when provided")
     })

     describe("getDocumentDownloadUrl", () => {
       it.todo("returns presigned URL with 5-min TTL")
       it.todo("verifies project membership before generating URL")
     })
   })
   ```

6. Create `tests/unit/pm-dashboard-synthesis.test.ts`:
   ```typescript
   import { describe, it, expect } from "vitest"

   describe("pm-dashboard-synthesis", () => {
     describe("PmDashboardData aggregation", () => {
       it.todo("aggregates stories by status")
       it.todo("aggregates stories by assignee")
       it.todo("aggregates questions by status")
       it.todo("computes knowledge coverage percentage")
       it.todo("computes sprint velocity from completed story points")
       it.todo("aggregates AI cost over time from SessionLog")
       it.todo("aggregates AI cost by feature area from sessionType")
       it.todo("aggregates QA pass/fail/blocked counts")
       it.todo("aggregates defects by severity")
       it.todo("stores result in Project.cachedBriefingContent.pmDashboard")
     })
   })
   ```
  </action>
  <verify>
    <automated>npx vitest run tests/unit/document-renderers.test.ts tests/unit/jira-sync.test.ts tests/unit/test-executions-actions.test.ts tests/unit/defects-actions.test.ts tests/unit/documents-actions.test.ts tests/unit/pm-dashboard-synthesis.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>6 additional test files created with failing .todo() stubs covering renderers, Jira sync, QA actions, document actions, and PM dashboard synthesis. All 12 test files exist with descriptive pending tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test-only plan, no production code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | accept | Wave 0 creates test stubs only -- no production code or trust boundaries |
</threat_model>

<verification>
- `npx vitest run --reporter=verbose` shows all 12 test files with pending/todo tests
- No test failures (all are .todo() stubs)
- Mock helpers export expected functions
</verification>

<success_criteria>
All 12 test stub files exist in tests/unit/ with descriptive .todo() tests covering every requirement (DOC-01 through DOC-04, QA-01 through QA-03, ADMIN-01 through ADMIN-03, PROJ-04, PROJ-05). Mock helpers for S3 and Jira are available for subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/05-document-generation-qa-and-administration/05-00-SUMMARY.md`
</output>
