---
phase: 05-document-generation-qa-and-administration
plan: 05
type: execute
wave: 2
depends_on: ["05-01", "05-04"]
files_modified:
  - src/app/(dashboard)/projects/[projectId]/documents/page.tsx
  - src/app/(dashboard)/projects/[projectId]/documents/[documentId]/page.tsx
  - src/components/documents/template-gallery.tsx
  - src/components/documents/generation-dialog.tsx
  - src/components/documents/generation-progress.tsx
  - src/components/documents/document-preview.tsx
  - src/components/documents/version-history-table.tsx
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "PM sees template gallery with branded template cards on Documents page"
    - "PM can configure sections, scope, and format before generating"
    - "Progress indicator shows generation steps during Inngest execution"
    - "Generated documents appear in version history table with download buttons"
    - "Document preview page shows rendered content with download options"
  artifacts:
    - path: "src/app/(dashboard)/projects/[projectId]/documents/page.tsx"
      provides: "Documents page with template gallery + version history"
    - path: "src/components/documents/template-gallery.tsx"
      provides: "Template card grid"
    - path: "src/components/documents/generation-dialog.tsx"
      provides: "Section/scope/format configuration dialog"
    - path: "src/components/documents/generation-progress.tsx"
      provides: "Real-time generation progress indicator"
    - path: "src/components/documents/version-history-table.tsx"
      provides: "Generated documents table with download"
    - path: "src/app/(dashboard)/projects/[projectId]/documents/[documentId]/page.tsx"
      provides: "Document preview page"
  key_links:
    - from: "src/components/documents/generation-dialog.tsx"
      to: "src/actions/documents.ts"
      via: "calls requestDocumentGeneration"
      pattern: "requestDocumentGeneration"
    - from: "src/components/documents/version-history-table.tsx"
      to: "src/actions/documents.ts"
      via: "calls getDocumentDownloadUrl"
      pattern: "getDocumentDownloadUrl"
---

<objective>
Build the document generation UI: template gallery page, generation configuration dialog with progress tracking, version history table, and document preview page.

Purpose: Provide PMs with a visual interface to select document templates, configure sections, monitor generation progress, and access generated documents with download and preview capabilities.
Output: Documents page with template gallery, generation dialog, progress indicator, version history, and document preview page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-document-generation-qa-and-administration/05-CONTEXT.md
@.planning/phases/05-document-generation-qa-and-administration/05-UI-SPEC.md

<interfaces>
From src/actions/documents.ts (created in Plan 04):
```typescript
export const requestDocumentGeneration = actionClient.schema(...).action(...)
export const getDocuments = actionClient.schema(...).action(...)
export const getDocumentDownloadUrl = actionClient.schema(...).action(...)
export const getDocumentTemplates = actionClient.schema(...).action(...)
```

From src/lib/documents/templates/types.ts (created in Plan 04):
```typescript
export interface DocumentTemplate {
  id: string; documentType: DocumentType; name: string; description: string;
  icon: string; supportedFormats: DocumentFormat[]; sections: TemplateSection[];
}
```

From src/lib/documents/templates/index.ts (created in Plan 04):
```typescript
export const DOCUMENT_TEMPLATES: DocumentTemplate[]
export function getTemplate(id: string): DocumentTemplate | undefined
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Documents page with template gallery and version history</name>
  <files>src/app/(dashboard)/projects/[projectId]/documents/page.tsx, src/components/documents/template-gallery.tsx, src/components/documents/version-history-table.tsx</files>
  <read_first>
    - src/app/(dashboard)/projects/[projectId]/dashboard/page.tsx (existing page pattern)
    - src/components/ui/card.tsx (Card component)
    - src/components/ui/table.tsx (Table component)
    - .planning/phases/05-document-generation-qa-and-administration/05-UI-SPEC.md (lines 277-316 for template gallery layout, lines 304-316 for version history table)
  </read_first>
  <action>
1. Create `src/components/documents/template-gallery.tsx`:
   - Client component
   - Props: `{ templates: DocumentTemplate[]; onGenerate: (templateId: string) => void }`
   - Responsive grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
   - Each template as a shadcn Card:
     - Card header: lucide icon (dynamically resolved from template.icon -- use a mapping: FileText, Presentation, FileSpreadsheet, FileCheck), template name (18px/600 heading)
     - Card body: description (14px/400 body, max 2 lines with `line-clamp-2`)
     - Card footer: "Generate Document" accent Button
   - Empty state: "No templates available" (unlikely but defensive)

2. Create `src/components/documents/version-history-table.tsx`:
   - Client component
   - Props: `{ documents: GeneratedDocumentRow[]; onDownload: (documentId: string) => void; onPreview: (documentId: string) => void }`
   - shadcn Table with columns:
     - Template (name, 14px/400)
     - Version (computed version number, e.g., "v3", 13px/400 metadata)
     - Generated By (displayName, 14px/400)
     - Date (formatted date, 13px/400 metadata)
     - Format (badge: DOCX/PPTX/PDF)
     - Actions (Download icon button with Download lucide icon, Preview link)
   - Download button calls onDownload which fetches presigned URL and opens in new tab
   - Preview link navigates to `/projects/[projectId]/documents/[documentId]`
   - Empty state: "No documents generated" heading, "Generate branded deliverables from your project's knowledge base. Choose a template to get started." body

3. Create `src/app/(dashboard)/projects/[projectId]/documents/page.tsx`:
   - Server component
   - Fetch templates via DOCUMENT_TEMPLATES import (static, no action needed)
   - Fetch generated documents via `getDocuments({ projectId })`
   - Page title: "Documents" (24px/600)
   - Section 1: "Templates" subheading + TemplateGallery
   - Separator
   - Section 2: "Generated Documents" subheading + VersionHistoryTable
   - Wrap interactive parts in client wrapper component that manages dialog state
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(dashboard)/projects/[projectId]/documents/page.tsx contains `Documents`
    - src/app/(dashboard)/projects/[projectId]/documents/page.tsx contains `DOCUMENT_TEMPLATES` or `getDocumentTemplates`
    - src/app/(dashboard)/projects/[projectId]/documents/page.tsx contains `getDocuments`
    - src/components/documents/template-gallery.tsx contains `"use client"`
    - src/components/documents/template-gallery.tsx contains `Generate Document`
    - src/components/documents/template-gallery.tsx contains `grid-cols-1 md:grid-cols-2`
    - src/components/documents/version-history-table.tsx contains `"use client"`
    - src/components/documents/version-history-table.tsx contains `onDownload`
    - src/components/documents/version-history-table.tsx contains `No documents generated`
  </acceptance_criteria>
  <done>Documents page renders template gallery as responsive card grid with "Generate Document" buttons, and version history table showing all generated documents with download actions and preview links.</done>
</task>

<task type="auto">
  <name>Task 2: Create generation dialog, progress indicator, and document preview page</name>
  <files>src/components/documents/generation-dialog.tsx, src/components/documents/generation-progress.tsx, src/components/documents/document-preview.tsx, src/app/(dashboard)/projects/[projectId]/documents/[documentId]/page.tsx</files>
  <read_first>
    - src/components/ui/dialog.tsx (Dialog component)
    - src/components/ui/checkbox.tsx (Checkbox component)
    - src/components/ui/progress.tsx (Progress bar)
    - .planning/phases/05-document-generation-qa-and-administration/05-UI-SPEC.md (lines 318-388 for generation dialog, progress, and preview layouts)
  </read_first>
  <action>
1. Create `src/components/documents/generation-dialog.tsx`:
   - Client component
   - Props: `{ template: DocumentTemplate | null; open: boolean; onOpenChange: (open: boolean) => void; projectId: string; epics: Array<{id, title}> }`
   - Dialog content (per UI-SPEC generation dialog layout):
     - Title: "Generate [template name]"
     - Section checkboxes: list template.sections with Checkbox + label. Required sections are checked and disabled. Optional sections default unchecked.
     - Scope selector: Select with options "All Epics" or individual epics (from epics prop)
     - Format selector: toggle buttons for template.supportedFormats (e.g., [Word] [PDF] [PowerPoint])
     - Footer: "Cancel" outline button, "Generate Document" accent button
   - On "Generate Document" click:
     - Collect selected sectionIds, format, scopeEpicId
     - Call `requestDocumentGeneration({ projectId, templateId: template.id, sectionIds, format })`
     - Switch dialog content to GenerationProgress component
   - Manages state: `configuring` -> `generating` -> `complete`

2. Create `src/components/documents/generation-progress.tsx`:
   - Client component
   - Props: `{ templateName: string; sections: string[]; onComplete: () => void }`
   - Shows progress during generation (per UI-SPEC progress layout):
     - Title: "Generating [template name]"
     - Progress bar (shadcn Progress component) -- uses polling to estimate progress
     - Step list showing section labels with check/pending icons
     - "Estimated time: ~30 seconds" metadata text
   - Uses SWR or polling interval to check for document completion:
     - Poll `getDocuments` every 3 seconds looking for new document with matching templateId
     - When found: show success state, auto-close dialog after 2 seconds
     - Show success toast via sonner: "Document generated. Download" with download link
   - Error state: show error message with retry option

3. Create `src/components/documents/document-preview.tsx`:
   - Client component
   - Props: `{ document: GeneratedDocumentDetail; downloadUrl: string }`
   - Renders document preview (per UI-SPEC preview layout):
     - For PDF: `<iframe src={downloadUrl} />` with 720px centered width
     - For DOCX/PPTX: show metadata card (title, format, generated by, date) with download buttons only (no inline preview for Office formats in V1)
   - Download buttons: "Download Word" / "Download PDF" / "Download PowerPoint" accent buttons

4. Create `src/app/(dashboard)/projects/[projectId]/documents/[documentId]/page.tsx`:
   - Server component
   - Fetch document details via `getDocument({ id: documentId })`
   - Fetch presigned download URL via `getDocumentDownloadUrl({ documentId })`
   - Back link: "< Back to Documents" linking to `/projects/[projectId]/documents`
   - Title: "[document.title] v[version number]" (24px/600)
   - Metadata line: "Generated by [name] on [date]" (13px/400 metadata)
   - Download buttons (top-right)
   - DocumentPreview component below
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/documents/generation-dialog.tsx contains `"use client"`
    - src/components/documents/generation-dialog.tsx contains `requestDocumentGeneration`
    - src/components/documents/generation-dialog.tsx contains `Generate Document`
    - src/components/documents/generation-dialog.tsx contains `Checkbox`
    - src/components/documents/generation-progress.tsx contains `"use client"`
    - src/components/documents/generation-progress.tsx contains `Progress`
    - src/components/documents/generation-progress.tsx contains `Document generated`
    - src/components/documents/document-preview.tsx contains `"use client"`
    - src/components/documents/document-preview.tsx contains `iframe`
    - src/app/(dashboard)/projects/[projectId]/documents/[documentId]/page.tsx contains `Back to Documents`
    - src/app/(dashboard)/projects/[projectId]/documents/[documentId]/page.tsx contains `getDocumentDownloadUrl`
  </acceptance_criteria>
  <done>Generation dialog shows section checkboxes, scope, and format selection. Progress indicator polls for completion with step-by-step visual feedback. Document preview page shows content preview for PDFs and download buttons for all formats. Success toast appears on generation completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> presigned URL | Downloads via time-limited S3 presigned URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | I (Info Disclosure) | document-preview | mitigate | Presigned URLs generated server-side only after project membership verification. 5-minute TTL. |
| T-05-13 | D (Denial of Service) | generation-dialog | mitigate | Rate limited by Inngest queue. UI disables Generate button during active generation. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- Documents page renders at `/projects/[projectId]/documents`
- Generation dialog opens from template card
- Preview page accessible at `/projects/[projectId]/documents/[documentId]`
</verification>

<success_criteria>
Document generation UI complete: template gallery shows branded cards, generation dialog configures sections/scope/format, progress indicator tracks Inngest execution, version history lists generated documents with download, and preview page renders documents with download options.
</success_criteria>

<output>
After completion, create `.planning/phases/05-document-generation-qa-and-administration/05-05-SUMMARY.md`
</output>
