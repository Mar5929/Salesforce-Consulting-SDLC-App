# BUG-042: Feature and Story detail pages are too basic — missing fields

- **Severity:** LOW
- **Phase:** 3 (Story Management and Sprint Intelligence)
- **Status:** Open

## Description

The feature detail page and story detail page are extremely sparse. The feature detail page only shows "Description" and "Acceptance Criteria" with nothing else. The story detail page shows a few more fields (displayId, title, status, assignee, sprint, storyPoints, description, acceptance criteria, persona, impacted components) but both feel incomplete for a work management tool.

## Location

- `src/app/(dashboard)/projects/[projectId]/work/[epicId]/[featureId]/feature-detail-client.tsx:1-144` — Feature detail page
- `src/app/(dashboard)/projects/[projectId]/work/[epicId]/[featureId]/stories/[storyId]/story-detail-client.tsx:1-291` — Story detail page

## Root Cause

The detail pages were built with minimal fields and no rich metadata display. Missing elements include:
- Feature: status, priority, assignee, story count, related epic context, creation/update dates, activity history
- Story: estimated vs actual hours, dependencies, related questions, conversation history, test coverage summary
- Both: comments/discussion thread, attachments, audit trail

## Fix

1. Review the Prisma schema for Feature and UserStory models to identify all available fields
2. Add missing field displays to both detail pages
3. Add metadata sidebar (status, priority, assignee, dates) in a consistent layout
4. Consider a tabbed layout similar to the story detail's existing Details/QA tabs
5. Add activity history or audit trail section

## Related Files

- `prisma/schema.prisma` — Feature and UserStory model definitions
- `src/app/(dashboard)/projects/[projectId]/work/[epicId]/[featureId]/feature-detail-client.tsx` — Feature detail
- `src/app/(dashboard)/projects/[projectId]/work/[epicId]/[featureId]/stories/[storyId]/story-detail-client.tsx` — Story detail
