---
phase: 03-story-management-and-sprint-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/(dashboard)/projects/[projectId]/work/page.tsx
  - src/app/(dashboard)/projects/[projectId]/work/[epicId]/page.tsx
  - src/app/(dashboard)/projects/[projectId]/work/[epicId]/[featureId]/page.tsx
  - src/app/(dashboard)/projects/[projectId]/backlog/page.tsx
  - src/components/work/work-breadcrumb.tsx
  - src/components/work/view-toggle.tsx
  - src/components/work/epic-table.tsx
  - src/components/work/epic-kanban.tsx
  - src/components/work/feature-table.tsx
  - src/components/work/story-table.tsx
  - src/components/work/story-kanban.tsx
  - src/components/work/story-form.tsx
  - src/components/work/component-selector.tsx
  - src/components/work/epic-form.tsx
  - src/components/work/feature-form.tsx
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - WORK-05
  - WORK-07

must_haves:
  truths:
    - "User can view epics in table and kanban views with toggle"
    - "User can drill down from epics to features to stories via breadcrumb navigation"
    - "User can create stories via slide-over panel with mandatory acceptance criteria"
    - "Story table shows displayId, title, status badge, priority, assignee, points, feature, sprint columns per D-03"
    - "Stories can attach free-text Salesforce components with impact type"
    - "Backlog page shows all unassigned stories across epics"
    - "Story status transitions are role-gated (PM lifecycle, dev execution)"
    - "Bulk actions allow multi-select for status change, sprint assignment, assignee change"
  artifacts:
    - path: "src/app/(dashboard)/projects/[projectId]/work/page.tsx"
      provides: "Epics list page with table/kanban toggle"
    - path: "src/components/work/story-form.tsx"
      provides: "Slide-over story creation/edit form"
      contains: "acceptanceCriteria"
    - path: "src/components/work/story-table.tsx"
      provides: "Story table with bulk actions and all D-03 columns"
    - path: "src/components/work/component-selector.tsx"
      provides: "Free-text component name + impact type selector"
    - path: "src/components/work/work-breadcrumb.tsx"
      provides: "Breadcrumb navigation for hierarchy drill-down"
  key_links:
    - from: "src/components/work/story-table.tsx"
      to: "src/actions/stories.ts"
      via: "server action calls for status change and bulk operations"
      pattern: "import.*updateStoryStatus.*from.*actions/stories"
    - from: "src/components/work/story-form.tsx"
      to: "src/actions/stories.ts"
      via: "createStory/updateStory calls"
      pattern: "import.*createStory.*from.*actions/stories"
---

<objective>
Build the work breakdown UI: hierarchical drill-down pages for epics, features, and stories with table/kanban views, breadcrumb navigation, story creation form (slide-over panel), component selector, bulk actions, and the backlog view.

Purpose: This is the primary work management interface -- the user's daily view for managing epics, features, and stories. All decisions D-01 through D-05, D-08, and D-09 are implemented here.
Output: Four route pages (work, epicId, featureId, backlog) and 12+ UI components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-story-management-and-sprint-intelligence/03-CONTEXT.md
@.planning/phases/03-story-management-and-sprint-intelligence/03-RESEARCH.md
@.planning/phases/03-story-management-and-sprint-intelligence/03-01-SUMMARY.md

@src/actions/epics.ts
@src/actions/features.ts
@src/actions/stories.ts
@src/lib/story-status-machine.ts
@src/components/questions/question-table.tsx
@src/components/questions/question-kanban.tsx
@src/components/questions/question-form.tsx
@src/components/shared/empty-state.tsx
@src/components/ui/sheet.tsx
@src/components/ui/table.tsx
@src/components/ui/badge.tsx
@src/components/ui/checkbox.tsx

<interfaces>
From src/actions/epics.ts (created in Plan 01):
```typescript
export const createEpic = actionClient.schema(...).action(...)
export const updateEpic = actionClient.schema(...).action(...)
export const deleteEpic = actionClient.schema(...).action(...)
export const getEpics = actionClient.schema(...).action(...)
export const getEpic = actionClient.schema(...).action(...)
```

From src/actions/features.ts (created in Plan 01):
```typescript
export const createFeature = actionClient.schema(...).action(...)
export const getFeatures = actionClient.schema(...).action(...)
```

From src/actions/stories.ts (created in Plan 01):
```typescript
export const createStory = actionClient.schema(...).action(...)
export const updateStory = actionClient.schema(...).action(...)
export const updateStoryStatus = actionClient.schema(...).action(...)
export const getStories = actionClient.schema(...).action(...)
```

From src/lib/story-status-machine.ts (created in Plan 01):
```typescript
export function canTransition(from: StoryStatus, to: StoryStatus, role: ProjectRole): boolean
export function getAvailableTransitions(from: StoryStatus, role: ProjectRole): StoryStatus[]
```

Prisma enums:
- StoryStatus: DRAFT | READY | SPRINT_PLANNED | IN_PROGRESS | IN_REVIEW | QA | DONE
- Priority: LOW | MEDIUM | HIGH | CRITICAL
- ImpactType: CREATE | MODIFY | DELETE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Work breakdown pages, breadcrumb, view toggle, epic/feature tables and kanbans</name>
  <files>src/app/(dashboard)/projects/[projectId]/work/page.tsx, src/app/(dashboard)/projects/[projectId]/work/[epicId]/page.tsx, src/app/(dashboard)/projects/[projectId]/work/[epicId]/[featureId]/page.tsx, src/app/(dashboard)/projects/[projectId]/backlog/page.tsx, src/components/work/work-breadcrumb.tsx, src/components/work/view-toggle.tsx, src/components/work/epic-table.tsx, src/components/work/epic-kanban.tsx, src/components/work/feature-table.tsx, src/components/work/epic-form.tsx, src/components/work/feature-form.tsx</files>
  <read_first>src/app/(dashboard)/projects/[projectId]/questions/page.tsx, src/components/questions/question-table.tsx, src/components/questions/question-kanban.tsx, src/components/questions/question-form.tsx, src/components/shared/empty-state.tsx, src/components/ui/sheet.tsx, src/components/ui/tabs.tsx, src/components/ui/dialog.tsx</read_first>
  <action>
**Breadcrumb component (src/components/work/work-breadcrumb.tsx) -- NEW FILE:**
Per D-01. Create a reusable breadcrumb component:
```typescript
"use client"
import Link from "next/link"
import { ChevronRight } from "lucide-react"
import { Fragment } from "react"

interface BreadcrumbSegment { label: string; href: string }

export function WorkBreadcrumb({ segments }: { segments: BreadcrumbSegment[] }) {
  return (
    <nav className="flex items-center gap-1.5 text-sm text-muted-foreground">
      {segments.map((seg, i) => (
        <Fragment key={seg.href}>
          {i > 0 && <ChevronRight className="h-4 w-4" />}
          {i === segments.length - 1 ? (
            <span className="font-medium text-foreground">{seg.label}</span>
          ) : (
            <Link href={seg.href} className="hover:text-foreground transition-colors">{seg.label}</Link>
          )}
        </Fragment>
      ))}
    </nav>
  )
}
```

**View toggle (src/components/work/view-toggle.tsx) -- NEW FILE:**
Per D-01. Reuse the same table/kanban toggle pattern from Phase 2 questions. Two buttons (LayoutList icon for table, Kanban icon for kanban), uses `nuqs` `useQueryState("view", { defaultValue: "table" })` for URL persistence.

**Epic form (src/components/work/epic-form.tsx) -- NEW FILE:**
Dialog form for creating/editing epics. Fields: name (required, text input), prefix (required, uppercase-only text input with `^[A-Z0-9]+$` pattern), description (optional, textarea). Uses react-hook-form + zod resolver. Calls `createEpic` or `updateEpic` server action.

**Feature form (src/components/work/feature-form.tsx) -- NEW FILE:**
Dialog form for creating/editing features. Fields: name (required), prefix (required), description (optional). Uses react-hook-form + zod. Calls `createFeature` or `updateFeature`.

**Epic table (src/components/work/epic-table.tsx) -- NEW FILE:**
Uses `@tanstack/react-table` with columns: prefix, name, status badge, features count, stories count, actions (edit/delete dropdown). Rows are clickable -- navigate to `/projects/${projectId}/work/${epicId}`. Follow the same pattern as question-table.tsx for column definitions, sorting, and empty state.

**Epic kanban (src/components/work/epic-kanban.tsx) -- NEW FILE:**
Three columns by EpicStatus: NOT_STARTED, IN_PROGRESS, COMPLETE. Each card shows prefix, name, feature count, story count. Cards are clickable to drill into epic.

**Feature table (src/components/work/feature-table.tsx) -- NEW FILE:**
Columns: prefix, name, status badge, stories count, actions. Rows clickable to navigate to `/projects/${projectId}/work/${epicId}/${featureId}`.

**Epics list page (src/app/(dashboard)/projects/[projectId]/work/page.tsx) -- NEW FILE:**
Server component. Calls `getEpics` action. Renders:
- Page header: "Work" title + "New Epic" button (opens epic form dialog)
- `<ViewToggle />` component
- Conditionally render `<EpicTable />` or `<EpicKanban />` based on view query param
- Empty state when no epics: "No epics yet. Create your first epic to start organizing work."

**Epic detail page (src/app/(dashboard)/projects/[projectId]/work/[epicId]/page.tsx) -- NEW FILE:**
Server component. Calls `getEpic` and `getFeatures` actions. Renders:
- `<WorkBreadcrumb segments={[{ label: "Work", href: ".../work" }, { label: epic.name, href: ".../work/${epicId}" }]} />`
- Feature table or kanban (same toggle pattern)
- "New Feature" button
- Context menu on epic header with "Generate Stories" option (placeholder for Plan 03, renders `toast("Coming soon")`)

**Feature detail page (src/app/(dashboard)/projects/[projectId]/work/[epicId]/[featureId]/page.tsx) -- NEW FILE:**
Server component. Calls `getStories({ projectId, featureId })`. Renders:
- `<WorkBreadcrumb segments={[Work, EpicName, FeatureName]} />`
- Story table (from Task 2) with view toggle
- "New Story" button opening slide-over form (from Task 2)

**Backlog page (src/app/(dashboard)/projects/[projectId]/backlog/page.tsx) -- NEW FILE:**
Per D-02. Server component. Calls `getStories({ projectId, unassigned: true })`. Renders:
- Page header: "Backlog" title
- Story table showing all stories where sprintId is null, across all epics
- Bulk action toolbar for sprint assignment
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(dashboard)/projects/[projectId]/work/page.tsx exists and contains `getEpics`
    - src/app/(dashboard)/projects/[projectId]/work/[epicId]/page.tsx exists and contains `WorkBreadcrumb`
    - src/app/(dashboard)/projects/[projectId]/work/[epicId]/[featureId]/page.tsx exists and contains `WorkBreadcrumb`
    - src/app/(dashboard)/projects/[projectId]/backlog/page.tsx exists and contains `unassigned`
    - src/components/work/work-breadcrumb.tsx contains `BreadcrumbSegment`
    - src/components/work/view-toggle.tsx contains `useQueryState`
    - src/components/work/epic-table.tsx contains `@tanstack/react-table`
    - src/components/work/epic-kanban.tsx contains `EpicStatus`
    - src/components/work/feature-table.tsx contains `@tanstack/react-table`
    - src/components/work/epic-form.tsx contains `createEpic`
    - src/components/work/feature-form.tsx contains `createFeature`
  </acceptance_criteria>
  <done>All four route pages render with breadcrumb navigation, table/kanban toggle, epic and feature CRUD forms, and the backlog view showing unassigned stories. Drill-down navigation works from Work > Epic > Feature.</done>
</task>

<task type="auto">
  <name>Task 2: Story table with bulk actions, story form (slide-over), component selector, story kanban</name>
  <files>src/components/work/story-table.tsx, src/components/work/story-kanban.tsx, src/components/work/story-form.tsx, src/components/work/component-selector.tsx</files>
  <read_first>src/components/questions/question-table.tsx, src/components/questions/question-form.tsx, src/components/ui/sheet.tsx, src/components/ui/checkbox.tsx, src/components/ui/select.tsx, src/components/ui/badge.tsx, src/actions/stories.ts, src/lib/story-status-machine.ts</read_first>
  <action>
**Story table (src/components/work/story-table.tsx) -- NEW FILE:**
Per D-03 and D-04. Uses `@tanstack/react-table`.

Columns per D-03:
1. Checkbox column for row selection (multi-select for bulk actions per D-04)
2. `displayId` -- monospace font, compact
3. `title` -- primary text, clickable to open story detail
4. `status` -- colored Badge component. Colors: DRAFT=gray, READY=blue, SPRINT_PLANNED=purple, IN_PROGRESS=yellow, IN_REVIEW=orange, QA=pink, DONE=green
5. `priority` -- Badge. CRITICAL=red, HIGH=orange, MEDIUM=yellow, LOW=gray
6. `assignee` -- display assignee displayName or "Unassigned" in muted text
7. `storyPoints` -- number or "-"
8. `feature` -- feature name or "-"
9. `sprint` -- sprint name or "Unassigned"

**Bulk action toolbar (inline at top of story table):**
Per D-04. Appears when 1+ rows selected. Shows:
- Selected count: "{N} selected"
- "Change Status" dropdown -- shows available statuses
- "Assign Sprint" dropdown -- loads available sprints for the project
- "Change Assignee" dropdown -- loads project members
- Each bulk action calls the corresponding server action in a loop for selected story IDs, then refreshes via `router.refresh()`

Use `table.getSelectedRowModel()` from TanStack for selection tracking.

**Story kanban (src/components/work/story-kanban.tsx) -- NEW FILE:**
Per D-01. Kanban columns by StoryStatus. Follow same pattern as question-kanban.tsx. Columns: DRAFT, READY, SPRINT_PLANNED, IN_PROGRESS, IN_REVIEW, QA, DONE. Each card shows: displayId, title, priority badge, assignee avatar (or initials), story points.

**Component selector (src/components/work/component-selector.tsx) -- NEW FILE:**
Per D-08. Inline multi-entry component for attaching Salesforce components to a story.
- Each entry has two fields: `componentName` (text input, free-text) and `impactType` (select: CREATE, MODIFY, DELETE)
- "Add Component" button appends a new row
- Each row has a delete (X) button to remove
- Controlled component: receives `value: Array<{ componentName: string, impactType: ImpactType }>` and `onChange` callback
- Used inside story-form.tsx as part of the form

**Story form (src/components/work/story-form.tsx) -- NEW FILE:**
Per D-05. Slide-over panel using shadcn Sheet component (opening from right).
Fields:
- `title` (required, text input)
- `epic` (required, select -- populated from epics in project. Pre-selected if opened from within an epic context)
- `feature` (optional, select -- filtered by selected epic. Pre-selected if opened from feature context)
- `persona` (optional, text input, placeholder "As a...")
- `description` (optional, textarea)
- `acceptanceCriteria` (required per WORK-02, textarea, placeholder "Given... When... Then...")
- `storyPoints` (optional, number input)
- `priority` (select: LOW, MEDIUM, HIGH, CRITICAL)
- `components` (ComponentSelector widget per D-08)

Uses react-hook-form with zod resolver. On submit: calls `createStory` server action, then for each component entry calls a `createStoryComponent` action (add to stories.ts if not there: `addStoryComponent({ projectId, storyId, componentName, impactType })`).

Dirty form protection per Pitfall 6: use `formState.isDirty` and `onOpenChange` to confirm before closing if unsaved changes exist.

Edit mode: When an existing story is passed as prop, pre-populate all fields and call `updateStory` on submit.

Status transition buttons: When editing, show current status badge and available transitions (from `getAvailableTransitions`). Each transition is a separate button that calls `updateStoryStatus`. Per D-09.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/work/story-table.tsx contains columns for `displayId`, `title`, `status`, `priority`, `assignee`, `storyPoints`, `feature`, `sprint`
    - src/components/work/story-table.tsx contains `getSelectedRowModel` (bulk selection)
    - src/components/work/story-table.tsx contains `Change Status` or `Assign Sprint` (bulk actions)
    - src/components/work/story-kanban.tsx contains `DRAFT` and `DONE` (status columns)
    - src/components/work/story-form.tsx contains `Sheet` import (slide-over per D-05)
    - src/components/work/story-form.tsx contains `acceptanceCriteria` field (WORK-02 mandatory)
    - src/components/work/story-form.tsx contains `isDirty` (dirty form protection per Pitfall 6)
    - src/components/work/story-form.tsx contains `getAvailableTransitions` (D-09 status transitions)
    - src/components/work/component-selector.tsx contains `componentName` and `impactType` (D-08)
    - src/components/work/component-selector.tsx contains `ImpactType` (CREATE/MODIFY/DELETE enum)
  </acceptance_criteria>
  <done>Story table displays all D-03 columns with bulk action toolbar (D-04). Story kanban shows status columns. Slide-over form has all D-05 fields with mandatory acceptance criteria (WORK-02), component selector (D-08), role-gated status transitions (D-09), and dirty form protection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI -> Server Actions | All form submissions validated server-side via Zod schemas |
| Bulk actions -> Database | Each bulk operation validates per-item permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Bulk status change | mitigate | Each status change in bulk loop validates via canTransition() server-side |
| T-03-06 | Elevation of Privilege | Story form edit | mitigate | Role check in updateStory server action (PM/SA can edit any, others own only) |
| T-03-07 | Information Disclosure | Backlog page | mitigate | getStories uses scopedPrisma -- only shows project's stories |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. Work page renders with epic table/kanban toggle
3. Drill-down navigation breadcrumbs render correctly at each level
4. Story form enforces mandatory acceptance criteria
5. Bulk action toolbar appears on row selection
</verification>

<success_criteria>
- All four route pages render and are navigable via drill-down
- Story table shows all 8 data columns per D-03 plus checkbox column
- Bulk actions work for status change, sprint assignment, assignee change
- Story form slide-over has all D-05 fields with validation
- Component selector allows free-text component + impact type entries
- Backlog shows unassigned stories across all epics
</success_criteria>

<output>
After completion, create `.planning/phases/03-story-management-and-sprint-intelligence/03-02-SUMMARY.md`
</output>
