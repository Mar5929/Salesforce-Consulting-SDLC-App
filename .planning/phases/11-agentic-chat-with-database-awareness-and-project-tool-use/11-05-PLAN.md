---
phase: 11-agentic-chat-with-database-awareness-and-project-tool-use
plan: 05
type: execute
wave: 3
depends_on:
  - "11-01-PLAN.md"
  - "11-02-PLAN.md"
  - "11-03-PLAN.md"
  - "11-04-PLAN.md"
files_modified:
  - src/app/api/chat/route.ts
  - src/components/chat/chat-interface.tsx
  - prisma/schema.prisma
  - tests/api/chat-route.test.ts
autonomous: true
requirements:
  - ACHAT-01
  - ACHAT-09
  - ACHAT-11
  - ACHAT-13
  - ACHAT-16
  - ACHAT-17
  - ACHAT-19

must_haves:
  truths:
    - "Chat route calls buildToolsForRole to get role-scoped tool set for every interactive session type"
    - "streamText is configured with stopWhen: stepCountIs(15) for multi-step chains"
    - "TRANSCRIPT_SESSION remains non-interactive (no tools, returns 400)"
    - "GENERAL_CHAT, STORY_SESSION, QUESTION_SESSION, BRIEFING_SESSION, ENRICHMENT_SESSION all receive full agentic tools"
    - "Database schema pushed — AGENTIC_CHAT enum value live in Neon"
    - "onFinish persists toolCalls JSON to ChatMessage for audit trail"
  artifacts:
    - path: "src/app/api/chat/route.ts"
      provides: "Upgraded chat route with full tool repertoire, stopWhen, role-gated tools"
      contains: "buildToolsForRole"
    - path: "prisma/schema.prisma"
      provides: "AGENTIC_CHAT in SessionLogTaskType (already added in Plan 01)"
  key_links:
    - from: "src/app/api/chat/route.ts"
      to: "src/lib/chat-tools/index.ts"
      via: "import { buildToolsForRole, buildAgenticSystemPrompt }"
      pattern: "buildToolsForRole"
    - from: "streamText call"
      to: "stopWhen: stepCountIs(15)"
      via: "AI SDK import"
      pattern: "stepCountIs"
---

<objective>
Upgrade the chat route to use the full agentic tool repertoire: wire buildToolsForRole and buildAgenticSystemPrompt, add stopWhen: stepCountIs(15) for multi-step chains, preserve all existing session-type behaviors, and push the schema migration so AGENTIC_CHAT is live.

This plan also configures the useChat client transport for the approval flow (sendAutomaticallyWhen) in chat-interface.tsx.

Purpose: All the tools built in prior plans are wired here into the actual API route. Without this plan, the tools exist in library code but are never called.

Output: Upgraded route.ts, schema push, chat-interface.tsx updated for approval flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-agentic-chat-with-database-awareness-and-project-tool-use/11-CONTEXT.md

@src/lib/project-scope.ts
@src/lib/auth.ts
</context>

<interfaces>
<!-- Key contracts the executor must implement — read source files via read_first before editing -->

AI SDK imports to add to route.ts:
- Add stepCountIs to the existing import from "ai"
- Add: import { buildToolsForRole, buildAgenticSystemPrompt } from "@/lib/chat-tools"

streamText shape after upgrade:
- tools: spread of agenticTools merged with session-specific tools
- stopWhen: stepCountIs(15)
- model string: preserve whatever is currently in the file, unchanged

Client useChat shape after upgrade:
- sendAutomaticallyWhen: lastAssistantMessageIsCompleteWithApprovalResponses
- addToolApprovalResponse extracted from useChat return value

Tool registry exports (from src/lib/chat-tools/index.ts, created in prior plans):
- buildToolsForRole(role: ProjectRole, projectId: string, memberId: string) returns Record of AI SDK tool objects
- buildAgenticSystemPrompt(projectId: string) returns Promise of string (lean ~500 token prompt)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade chat route with agentic tools, stopWhen, and approval flow client config</name>
  <files>
    src/app/api/chat/route.ts
    src/components/chat/chat-interface.tsx
    tests/api/chat-route.test.ts
  </files>
  <read_first>
    - src/app/api/chat/route.ts (READ FULL FILE — understand the entire current implementation before modifying; you are extending, not replacing)
    - src/components/chat/chat-interface.tsx (READ FULL FILE — understand useChat setup, DefaultChatTransport, existing session type props and what is already destructured from useChat)
    - src/lib/chat-tools/index.ts (buildToolsForRole and buildAgenticSystemPrompt signatures)
    - node_modules/ai/dist/index.d.ts (confirm stepCountIs and lastAssistantMessageIsCompleteWithApprovalResponses are exported — if not found, check node_modules/ai/package.json exports field for the correct import path)
    - tests/api/ (check if directory exists; look at an existing test for vitest patterns if so)
  </read_first>
  <behavior>
    chat-route.test.ts:
    - Test: The route module exports a POST function
    - Test: buildToolsForRole is importable from "@/lib/chat-tools" and is a function
    - Test: buildAgenticSystemPrompt is importable from "@/lib/chat-tools" and is a function
    NOTE: Full integration tests require live DB and AI — this file provides structural smoke tests only.
  </behavior>
  <action>
    **Step 1: Upgrade src/app/api/chat/route.ts**

    Read the full existing route.ts first. Then make these targeted changes:

    1. Extend the existing "ai" import to include stepCountIs:
       Change: import { streamText, tool, convertToModelMessages } from "ai"
       To:     import { streamText, tool, convertToModelMessages, stepCountIs } from "ai"

    2. Add after the existing AI SDK imports:
       import { buildToolsForRole, buildAgenticSystemPrompt } from "@/lib/chat-tools"

    3. After the `const member = await getCurrentMember(projectId)` line, add:
       // Build role-scoped tools for this user (D-08, D-10)
       const agenticTools = buildToolsForRole(member.role, projectId, member.id)

    4. In the system prompt selection chain, change ONLY the final else branch — the one
       that currently calls assembleEnhancedChatContext and buildSmartChatSystemPrompt.
       Replace that entire else block with:
         // General chat: lean agentic prompt — tools provide data on demand (D-15)
         systemPrompt = await buildAgenticSystemPrompt(projectId)
       All other branches (STORY_SESSION, BRIEFING_SESSION, ENRICHMENT_SESSION,
       QUESTION_SESSION) must remain completely unchanged.

    5. In the streamText call, replace the tools conditional spread with:
         tools: {
           ...agenticTools,
           ...(isStorySession ? { create_story_draft: storyDraftTool } : {}),
           ...(isEnrichmentSession ? { create_enrichment_suggestion: enrichmentSuggestionTool } : {}),
         },
         stopWhen: stepCountIs(15),
       The model identifier string inside anthropic(...) must NOT be changed — preserve it exactly.

    6. Keep the existing onFinish handler unchanged.

    **Step 2: Update src/components/chat/chat-interface.tsx**

    Read the full file first. Then:

    a. Locate the import for useChat. Note which package it comes from. Add
       lastAssistantMessageIsCompleteWithApprovalResponses to that same import.
       If that symbol is not exported from the detected package, check
       node_modules/ai/dist/index.d.ts for the correct import path. Do not guess.

    b. In the useChat options object, add:
       sendAutomaticallyWhen: lastAssistantMessageIsCompleteWithApprovalResponses

    c. Add addToolApprovalResponse to the destructured values from useChat.

    d. Pass addToolApprovalResponse as a new prop to the MessageList component render.
       Update the MessageList props interface to accept:
       addToolApprovalResponse?: (params: { id: string; approved: boolean }) => void

    **Step 3: Create tests/api/chat-route.test.ts**

    Create the directory tests/api/ if it does not exist. Create the file with these tests:

    import { describe, it, expect } from "vitest"

    describe("Chat route structure", () => {
      it("POST handler is exported from route module", async () => {
        const mod = await import("@/app/api/chat/route")
        expect(typeof mod.POST).toBe("function")
      })

      it("buildToolsForRole is importable from chat-tools", async () => {
        const mod = await import("@/lib/chat-tools")
        expect(typeof mod.buildToolsForRole).toBe("function")
      })

      it("buildAgenticSystemPrompt is importable from chat-tools", async () => {
        const mod = await import("@/lib/chat-tools")
        expect(typeof mod.buildAgenticSystemPrompt).toBe("function")
      })
    })
  </action>
  <verify>
    <automated>cd /Users/michaelrihm/Documents/Salesforce-Consulting-SDLC-App && npx vitest run tests/api/chat-route.test.ts tests/lib/chat-tools/ --reporter=verbose</automated>
  </verify>
  <done>
    - All tests pass (chat-route.test.ts + all chat-tools tests)
    - route.ts contains "buildToolsForRole" (grep-verifiable)
    - route.ts contains "stopWhen: stepCountIs(15)" (grep-verifiable)
    - chat-interface.tsx contains "sendAutomaticallyWhen" and "addToolApprovalResponse"
    - npx tsc --noEmit: no type errors
  </done>
</task>

<task type="auto">
  <name>[BLOCKING] Task 2: Push schema migration (AGENTIC_CHAT enum) to database</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (verify AGENTIC_CHAT is present in SessionLogTaskType enum — added in Plan 01 Task 1)
  </read_first>
  <action>
    Run the Prisma schema push to apply the AGENTIC_CHAT enum addition to the live Neon database.

    Run: npx prisma db push

    If the command prompts about data loss (unlikely for an enum addition only), run:
    npx prisma db push --accept-data-loss

    After the push completes, regenerate the Prisma client:
    npx prisma generate

    Verify the generated client includes AGENTIC_CHAT:
    grep -n "AGENTIC_CHAT" src/generated/prisma/index.d.ts
  </action>
  <verify>
    <automated>grep -n "AGENTIC_CHAT" /Users/michaelrihm/Documents/Salesforce-Consulting-SDLC-App/src/generated/prisma/index.d.ts</automated>
  </verify>
  <done>
    - npx prisma db push exits 0
    - grep AGENTIC_CHAT src/generated/prisma/index.d.ts returns a match
    - Prisma client regenerated with new enum value
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request body to tool selection | role comes from getCurrentMember() (Clerk session), never from request body |
| client approval to execute | needsApproval structural gate; client sends addToolApprovalResponse; SDK enforces execution only after approval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-18 | Elevation of Privilege | Chat route tool assembly | mitigate | member.role from getCurrentMember(projectId) is Clerk-verified server-side — client cannot inject a different role |
| T-11-19 | Denial of Service | Multi-step tool chains | mitigate | stopWhen: stepCountIs(15) hard cap — AI cannot loop indefinitely regardless of prompt content |
| T-11-20 | Tampering | TRANSCRIPT_SESSION blocking | mitigate | Existing check for TRANSCRIPT_SESSION returning 400 is preserved — transcript sessions cannot receive tool calls |
| T-11-21 | Information Disclosure | onFinish toolCalls JSON | accept | toolCalls stored in ChatMessage — scoped to conversation, project-member access controlled by existing auth |
</threat_model>

<verification>
1. grep -n "buildToolsForRole" src/app/api/chat/route.ts — present
2. grep -n "stopWhen: stepCountIs" src/app/api/chat/route.ts — present
3. grep -n "sendAutomaticallyWhen" src/components/chat/chat-interface.tsx — present
4. grep -n "AGENTIC_CHAT" src/generated/prisma/index.d.ts — match found
5. npx vitest run tests/lib/chat-tools/ tests/api/ --reporter=verbose — all tests pass
6. npx tsc --noEmit — no type errors
</verification>

<success_criteria>
- Chat route passes role-scoped tools to every interactive session type
- stopWhen: stepCountIs(15) enforces the 15-call limit per AI turn
- TRANSCRIPT_SESSION still returns 400 (non-interactive, unchanged)
- Schema pushed: AGENTIC_CHAT visible in generated Prisma client
- useChat in chat-interface.tsx configured for approval auto-resubmit
- All tests green
</success_criteria>

<output>
After completion, create .planning/phases/11-agentic-chat-with-database-awareness-and-project-tool-use/11-05-SUMMARY.md
</output>
