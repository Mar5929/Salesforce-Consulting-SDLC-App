---
phase: 05-document-generation-qa-and-administration
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/app/(dashboard)/projects/[projectId]/defects/page.tsx
  - src/components/defects/defect-table.tsx
  - src/components/defects/defect-kanban.tsx
  - src/components/defects/defect-create-sheet.tsx
  - src/components/defects/defect-filters.tsx
  - src/components/qa/test-execution-table.tsx
  - src/components/qa/record-result-form.tsx
autonomous: true
requirements:
  - QA-01
  - QA-02
  - QA-03

must_haves:
  truths:
    - "QA tab on story detail shows test cases with latest execution results"
    - "QA can record Pass/Fail/Blocked results with notes inline"
    - "Failed test rows show Create Defect action that pre-populates defect form"
    - "Dedicated Defects page shows all project defects in table and kanban views"
    - "Defect creation uses slide-over panel (Sheet) with required fields"
    - "Defect kanban columns map to lifecycle: Open, In Progress, Fixed, Verified, Closed"
  artifacts:
    - path: "src/app/(dashboard)/projects/[projectId]/defects/page.tsx"
      provides: "Defects list page with table/kanban toggle"
    - path: "src/components/defects/defect-table.tsx"
      provides: "Defect table view with sortable columns"
    - path: "src/components/defects/defect-kanban.tsx"
      provides: "Defect kanban view with lifecycle columns"
    - path: "src/components/defects/defect-create-sheet.tsx"
      provides: "Slide-over defect creation panel"
    - path: "src/components/qa/test-execution-table.tsx"
      provides: "Test case table with execution results for story QA tab"
  key_links:
    - from: "src/components/qa/test-execution-table.tsx"
      to: "src/actions/test-executions.ts"
      via: "calls getStoryTestCases and recordTestExecution"
      pattern: "getStoryTestCases|recordTestExecution"
    - from: "src/components/defects/defect-table.tsx"
      to: "src/actions/defects.ts"
      via: "calls getDefects and transitionDefectStatus"
      pattern: "getDefects|transitionDefectStatus"
---

<objective>
Build the QA workflow UI: test execution table for story detail QA tab, defect list page with table/kanban toggle, and defect creation slide-over panel.

Purpose: Enable QA team members to record test results on stories and manage defects through their lifecycle with the same UI patterns established in Phases 2-3 (table+kanban toggle, slide-over creation panel).
Output: Defects page, story QA tab components, defect creation sheet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-document-generation-qa-and-administration/05-CONTEXT.md
@.planning/phases/05-document-generation-qa-and-administration/05-UI-SPEC.md

<interfaces>
From src/actions/test-executions.ts (created in Plan 02):
```typescript
export const createTestCase = actionClient.schema(...).action(...)
export const recordTestExecution = actionClient.schema(...).action(...)
export const getStoryTestCases = actionClient.schema(...).action(...)
export const updateTestCase = actionClient.schema(...).action(...)
export const deleteTestCase = actionClient.schema(...).action(...)
```

From src/actions/defects.ts (created in Plan 02):
```typescript
export const createDefect = actionClient.schema(...).action(...)
export const updateDefect = actionClient.schema(...).action(...)
export const transitionDefectStatus = actionClient.schema(...).action(...)
export const getDefects = actionClient.schema(...).action(...)
export const deleteDefect = actionClient.schema(...).action(...)
```

From src/lib/defect-status-machine.ts (created in Plan 01):
```typescript
export const DEFECT_STATUS_LABELS: Record<DefectStatus, string>
export function getNextDefectStatuses(current: DefectStatus, role: ProjectRole): DefectStatus[]
```

From src/components/work/view-toggle.tsx (reusable pattern):
```typescript
// nuqs-based table/kanban toggle
export function ViewToggle() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create story QA tab components (test execution table + record result form)</name>
  <files>src/components/qa/test-execution-table.tsx, src/components/qa/record-result-form.tsx</files>
  <read_first>
    - src/components/work/view-toggle.tsx (for reusable toggle pattern)
    - src/app/(dashboard)/projects/[projectId]/work/ (existing story detail page structure to understand where QA tab goes)
    - src/components/ui/table.tsx (shadcn table component)
    - src/components/ui/badge.tsx (for result badges)
    - src/components/ui/tabs.tsx (for story detail tabs)
    - .planning/phases/05-document-generation-qa-and-administration/05-UI-SPEC.md (lines 390-413 for Story QA Tab layout)
  </read_first>
  <action>
1. Create `src/components/qa/test-execution-table.tsx`:
   - Client component (`"use client"`)
   - Props: `{ storyId: string; projectId: string; memberRole: string }`
   - Fetches test cases via `getStoryTestCases({ storyId })` on mount using useEffect + useState
   - Renders shadcn Table with columns: Test Case (title), Result (badge), Tester (displayName), Date (13px metadata), Notes (14px body)
   - Result badges use semantic colors from UI-SPEC:
     - PASS: solid badge with `bg-green-500 text-white`
     - FAIL: solid badge with `bg-red-500 text-white`
     - BLOCKED: solid badge with `bg-amber-500 text-white`
     - Not Run (no execution): outline badge with `text-muted-foreground`
   - Failed test rows show inline "Create Defect" link button that calls `onCreateDefectFromTest(testCase)` callback prop
   - Empty state: heading "No test results", body "Record test execution results to track quality. Mark each test case as Pass, Fail, or Blocked.", action button "Record Result"
   - "Record Result" accent button at top-right opens the RecordResultForm inline

2. Create `src/components/qa/record-result-form.tsx`:
   - Client component
   - Props: `{ storyId: string; projectId: string; onComplete: () => void }`
   - Two-step form:
     Step 1: Add/select test case -- form fields: title (required), steps (optional), expectedResult (required), testType (select: HAPPY_PATH, EDGE_CASE, NEGATIVE, BULK)
     Step 2: Record result -- result (PASS/FAIL/BLOCKED radio group), notes (textarea), environment (optional input)
   - Uses react-hook-form with Zod resolver
   - On submit: calls `createTestCase` then `recordTestExecution` sequentially
   - Shows success toast via sonner on completion
   - Calls `onComplete()` to refresh the table
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/qa/test-execution-table.tsx contains `"use client"`
    - src/components/qa/test-execution-table.tsx contains `getStoryTestCases`
    - src/components/qa/test-execution-table.tsx contains `bg-green-500` (Pass badge)
    - src/components/qa/test-execution-table.tsx contains `bg-red-500` (Fail badge)
    - src/components/qa/test-execution-table.tsx contains `bg-amber-500` (Blocked badge)
    - src/components/qa/test-execution-table.tsx contains `Create Defect`
    - src/components/qa/test-execution-table.tsx contains `No test results`
    - src/components/qa/record-result-form.tsx contains `"use client"`
    - src/components/qa/record-result-form.tsx contains `createTestCase`
    - src/components/qa/record-result-form.tsx contains `recordTestExecution`
    - src/components/qa/record-result-form.tsx contains `useForm`
  </acceptance_criteria>
  <done>Story QA tab components render test cases with color-coded result badges, inline "Create Defect" on failed tests, and a two-step form for recording new test executions. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create defects list page with table/kanban toggle and creation sheet</name>
  <files>src/app/(dashboard)/projects/[projectId]/defects/page.tsx, src/components/defects/defect-table.tsx, src/components/defects/defect-kanban.tsx, src/components/defects/defect-create-sheet.tsx, src/components/defects/defect-filters.tsx</files>
  <read_first>
    - src/app/(dashboard)/projects/[projectId]/questions/page.tsx (pattern for table+kanban toggle page)
    - src/components/work/view-toggle.tsx (nuqs-based toggle)
    - src/components/ui/sheet.tsx (slide-over panel)
    - src/components/ui/select.tsx (filter selects)
    - .planning/phases/05-document-generation-qa-and-administration/05-UI-SPEC.md (lines 415-489 for defects list, kanban, and creation sheet)
  </read_first>
  <action>
1. Create `src/components/defects/defect-filters.tsx`:
   - Client component
   - Filter bar with 4 selects: Status (all DefectStatus values, label via DEFECT_STATUS_LABELS), Severity (LOW/MEDIUM/HIGH/CRITICAL), Assignee (team members), Story (stories list)
   - Uses nuqs `useQueryState` for URL-persisted filter values (pattern from Phase 3)
   - Each filter is a shadcn Select with "All" as default option
   - Props: `{ projectId: string; members: Array<{id, displayName}>; stories: Array<{id, displayId, title}> }`

2. Create `src/components/defects/defect-table.tsx`:
   - Client component
   - Props: `{ defects: DefectRow[]; onTransition: (id, status) => void; onEdit: (defect) => void }`
   - shadcn Table with columns: ID (displayId, 13px metadata), Title (14px body, clickable), Severity (colored badge per UI-SPEC), Status (colored badge per UI-SPEC: Open=outline muted, In Progress=solid primary, Fixed=solid amber, Verified=solid green, Closed=outline secondary), Story (displayId link), Assignee (displayName), Created (date, 13px metadata)
   - Kebab menu per row with: Edit, Change Status (submenu with available transitions), Delete
   - Severity badge colors: Critical=#EF4444 solid, High=#F97316 solid, Medium=#F59E0B solid, Low=outline muted
   - Status badge colors: per UI-SPEC defect status badge section

3. Create `src/components/defects/defect-kanban.tsx`:
   - Client component
   - Props: `{ defects: DefectRow[]; onTransition: (id, status) => void; onEdit: (defect) => void }`
   - 5 columns: Open, In Progress, Fixed, Verified, Closed (using DEFECT_STATUS_LABELS for column headers)
   - Cards show: displayId, title (14px/400), severity badge, assignee avatar or initials, linked story displayId
   - Cards not draggable in V1 (drag-and-drop deferred) -- use status transition via card kebab menu instead

4. Create `src/components/defects/defect-create-sheet.tsx`:
   - Client component
   - Props: `{ projectId: string; open: boolean; onOpenChange: (open: boolean) => void; onCreated: () => void; prefill?: { storyId?: string; testCaseId?: string; title?: string } }`
   - Sheet (slide-over from right) with form fields per UI-SPEC:
     - Title (required, input)
     - Description (required, textarea) -- maps to `stepsToReproduce` in the action
     - Severity (required, select: Critical/High/Medium/Low)
     - Linked Story (optional, story search select)
     - Assignee (optional, team member select)
     - Steps to Reproduce (required, textarea) -- maps to `stepsToReproduce`
     - Expected vs Actual (two textareas for `expectedBehavior` and `actualBehavior`)
   - Note: The schema has separate `stepsToReproduce`, `expectedBehavior`, and `actualBehavior` fields. Map the UI accordingly:
     - "Steps to Reproduce" textarea -> stepsToReproduce
     - "Expected Behavior" textarea -> expectedBehavior
     - "Actual Behavior" textarea -> actualBehavior
   - Pre-populated when created from failed test case (via `prefill` prop)
   - Uses react-hook-form + Zod resolver
   - On submit: calls `createDefect` action, shows success toast, calls `onCreated()`
   - "Create Defect" accent button, "Cancel" outline button

5. Create `src/app/(dashboard)/projects/[projectId]/defects/page.tsx`:
   - Server component page
   - Fetches defects via `getDefects({ projectId })` server-side
   - Fetches team members and stories for filter selects
   - Page title "Defects" (24px/600)
   - Layout: title row with "Create Defect" accent button + ViewToggle (table/kanban)
   - Below: DefectFilters, then DefectTable or DefectKanban based on view toggle
   - Empty state per UI-SPEC: "No defects reported" heading, "Defects are created from failed test cases or manually..." body, "Create Defect" button
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(dashboard)/projects/[projectId]/defects/page.tsx exists and is a valid page component
    - src/app/(dashboard)/projects/[projectId]/defects/page.tsx contains `Defects`
    - src/components/defects/defect-table.tsx contains `"use client"`
    - src/components/defects/defect-table.tsx contains `displayId`
    - src/components/defects/defect-table.tsx contains `#EF4444` (Critical badge)
    - src/components/defects/defect-kanban.tsx contains `"use client"`
    - src/components/defects/defect-kanban.tsx contains `DEFECT_STATUS_LABELS`
    - src/components/defects/defect-create-sheet.tsx contains `Sheet`
    - src/components/defects/defect-create-sheet.tsx contains `createDefect`
    - src/components/defects/defect-create-sheet.tsx contains `useForm`
    - src/components/defects/defect-create-sheet.tsx contains `prefill`
    - src/components/defects/defect-filters.tsx contains `useQueryState`
    - src/components/defects/defect-filters.tsx contains `DEFECT_STATUS_LABELS`
  </acceptance_criteria>
  <done>Defects page shows all project defects in table (default) or kanban view with status/severity/assignee/story filters. Defect creation via slide-over Sheet panel supports pre-population from failed test cases. All badge colors match UI-SPEC. TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server action | All data mutations go through server actions with auth checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | S (Spoofing) | defects page | mitigate | Page loads data through server actions that verify project membership |
| T-05-08 | T (Tampering) | defect-kanban | accept | V1 kanban uses kebab menu for status transitions (validated server-side), not drag-and-drop |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- Defects page renders at `/projects/[projectId]/defects`
- Table and kanban views switch correctly
- Defect creation sheet opens and submits
</verification>

<success_criteria>
QA workflow UI is fully functional: story QA tab displays test cases with color-coded results and inline defect creation, dedicated Defects page provides table/kanban views with filters, and defect creation uses the established slide-over panel pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/05-document-generation-qa-and-administration/05-03-SUMMARY.md`
</output>
