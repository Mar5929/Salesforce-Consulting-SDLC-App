---
phase: 02-discovery-and-knowledge-brain
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
autonomous: true
gap_closure: true
requirements: [QUES-05, NOTF-03]

must_haves:
  truths:
    - "QuestionStatus enum contains OPEN, SCOPED, OWNED, ANSWERED, REVIEWED, PARKED"
    - "Notification model has a priority field with NotificationPriority enum (LOW, NORMAL, HIGH, URGENT)"
    - "Notification model index includes priority for efficient ordering"
    - "Prisma client is regenerated and schema is pushed to database"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Updated QuestionStatus enum and Notification priority field"
      contains: "SCOPED"
  key_links:
    - from: "prisma/schema.prisma"
      to: "database"
      via: "prisma db push"
      pattern: "NotificationPriority"
---

<objective>
Schema migration for question lifecycle and notification priority

Purpose: Close verification gaps 2 and 5 at the schema level. The QuestionStatus enum needs SCOPED, OWNED, and REVIEWED states for the 5-state lifecycle (QUES-05). The Notification model needs a priority field for priority-based ordering (NOTF-03). Both are schema-level blockers that must be resolved before UI and logic changes.

Output: Updated Prisma schema with both changes pushed to the database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QuestionStatus enum values and Notification priority field to Prisma schema</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma</read_first>
  <action>
1. Update the QuestionStatus enum (currently at ~line 100) from:
```
enum QuestionStatus {
  OPEN
  ANSWERED
  PARKED
}
```
to:
```
enum QuestionStatus {
  OPEN
  SCOPED
  OWNED
  ANSWERED
  REVIEWED
  PARKED
}
```

2. Add a new NotificationPriority enum after the existing NotificationType enum (~line 315):
```
enum NotificationPriority {
  LOW
  NORMAL
  HIGH
  URGENT
}
```

3. Add a priority field to the Notification model (currently at ~line 969). Add after the `type` field:
```
priority    NotificationPriority   @default(NORMAL)
```

4. Update the Notification model's index from:
```
@@index([recipientId, isRead, createdAt])
```
to:
```
@@index([recipientId, isRead, priority, createdAt])
```
This enables efficient queries that sort by priority DESC then createdAt DESC.
  </action>
  <verify>
    <automated>grep -c "SCOPED" prisma/schema.prisma && grep -c "OWNED" prisma/schema.prisma && grep -c "REVIEWED" prisma/schema.prisma && grep -c "NotificationPriority" prisma/schema.prisma && grep -c "priority.*NotificationPriority" prisma/schema.prisma && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `SCOPED` in QuestionStatus enum
    - prisma/schema.prisma contains `OWNED` in QuestionStatus enum
    - prisma/schema.prisma contains `REVIEWED` in QuestionStatus enum
    - prisma/schema.prisma contains `enum NotificationPriority {`
    - prisma/schema.prisma contains `priority    NotificationPriority   @default(NORMAL)`
    - prisma/schema.prisma contains `@@index([recipientId, isRead, priority, createdAt])`
    - `npx prisma validate` exits 0
  </acceptance_criteria>
  <done>QuestionStatus has 6 values (OPEN, SCOPED, OWNED, ANSWERED, REVIEWED, PARKED). Notification model has priority field with NotificationPriority enum. Schema validates successfully.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push schema changes to database</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma</read_first>
  <action>
Run the Prisma schema push command to apply the enum additions and new field to the database.

1. First run `npx prisma generate` to regenerate the Prisma client with new types.
2. Then run `npx prisma db push` to push schema changes to the database.
   - If the push requires interactive prompts about data loss, use `npx prisma db push --accept-data-loss` since we are only ADDING enum values and a new column with a default — this is non-destructive.
   - Adding enum values and adding a nullable/defaulted column should NOT require --accept-data-loss, but use it as fallback if the non-interactive push fails.
3. Verify the push succeeded by checking exit code.
  </action>
  <verify>
    <automated>npx prisma generate && npx prisma db push 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma generate` exits 0
    - `npx prisma db push` exits 0 with "Your database is now in sync with your Prisma schema" or similar success message
    - The generated Prisma client includes QuestionStatus.SCOPED, QuestionStatus.OWNED, QuestionStatus.REVIEWED
    - The generated Prisma client includes NotificationPriority enum
  </acceptance_criteria>
  <done>Database schema is in sync with Prisma schema. New enum values and priority field are available for use in application code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema migration | Adding enum values and a column with default — no data loss risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | T (Tampering) | prisma db push | accept | Schema push is a dev/deploy operation, not runtime. Adding enum values and defaulted column is non-destructive. |
</threat_model>

<verification>
- `npx prisma validate` passes
- `npx prisma db push` succeeds
- grep confirms SCOPED, OWNED, REVIEWED in QuestionStatus enum
- grep confirms NotificationPriority enum exists
- grep confirms priority field on Notification model
</verification>

<success_criteria>
Schema changes applied to database. QuestionStatus has 6 states. Notification has priority field. Prisma client regenerated with new types.
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-and-knowledge-brain/02-10-SUMMARY.md`
</output>
