---
phase: 02-discovery-and-knowledge-brain
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/lib/agent-harness/context/project-summary.ts
  - src/lib/agent-harness/context/questions.ts
  - src/lib/agent-harness/context/decisions.ts
  - src/lib/agent-harness/context/article-summaries.ts
  - src/lib/agent-harness/context/blocking-relationships.ts
  - src/lib/agent-harness/context/index.ts
  - src/lib/agent-harness/tasks/transcript-processing.ts
  - src/lib/agent-harness/tasks/question-answering.ts
  - src/lib/agent-harness/tasks/dashboard-synthesis.ts
  - tests/lib/agent-harness/context.test.ts
autonomous: true
requirements: [AGENT-04, KNOW-05]

must_haves:
  truths:
    - "Context loaders fetch project data scoped to projectId and return structured context within token budgets"
    - "Two-pass retrieval loads article summaries first, then full content for the most relevant articles"
    - "Each task definition specifies its required context, token budget, tools, and validation rules"
    - "Context assembly respects per-task token budgets by truncating or selecting data"
  artifacts:
    - path: "src/lib/agent-harness/context/project-summary.ts"
      provides: "getProjectSummary() context loader"
      exports: ["getProjectSummary"]
    - path: "src/lib/agent-harness/context/questions.ts"
      provides: "getOpenQuestions(), getQuestionContext() loaders"
      exports: ["getOpenQuestions", "getQuestionContext"]
    - path: "src/lib/agent-harness/context/article-summaries.ts"
      provides: "getArticleSummaries(), getRelevantArticles() two-pass loader"
      exports: ["getArticleSummaries", "getRelevantArticles"]
    - path: "src/lib/agent-harness/tasks/transcript-processing.ts"
      provides: "TRANSCRIPT_PROCESSING task definition"
      exports: ["transcriptProcessingTask"]
    - path: "src/lib/agent-harness/tasks/question-answering.ts"
      provides: "QUESTION_ANSWERING task definition"
      exports: ["questionAnsweringTask"]
    - path: "src/lib/agent-harness/tasks/dashboard-synthesis.ts"
      provides: "DASHBOARD_SYNTHESIS task definition"
      exports: ["dashboardSynthesisTask"]
  key_links:
    - from: "src/lib/agent-harness/context/*.ts"
      to: "prisma"
      via: "scopedPrisma queries"
      pattern: "scopedPrisma.*find"
    - from: "src/lib/agent-harness/tasks/*.ts"
      to: "src/lib/agent-harness/context/"
      via: "contextLoader function reference"
      pattern: "contextLoader.*getProjectSummary|getOpenQuestions"
---

<objective>
Context assembly (Layer 3) and task definitions (Layer 1) for the agent harness

Purpose: Context assembly is what makes the AI smart about each project -- it loads the right data (questions, decisions, articles, summaries) within token budgets so every AI call gets relevant project context without overflowing the context window. Task definitions wire context loaders to specific AI behaviors (transcript processing, question answering, dashboard synthesis).

Output: Five context loaders (project summary, questions, decisions, article summaries, blocking relationships), three task definitions (transcript processing, question answering, dashboard synthesis), and two-pass article retrieval (D-19).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-discovery-and-knowledge-brain/02-CONTEXT.md
@.planning/phases/02-discovery-and-knowledge-brain/02-RESEARCH.md
@.planning/phases/02-discovery-and-knowledge-brain/02-01-SUMMARY.md

@SF-Consulting-AI-Framework-PRD-v2.1.md (Section 6: AI Agent Harness, Section 4.2: Token Budgets)
@SESSION-3-TECHNICAL-SPEC.md (Section 3.3: Context Assembly, Section 3.4: Tool Definitions, Section 4.2: Token Budgets)

@src/lib/agent-harness/types.ts
@src/lib/agent-harness/engine.ts
@src/lib/project-scope.ts
@src/lib/db.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Context loaders (Layer 3) with two-pass article retrieval</name>
  <files>
    src/lib/agent-harness/context/project-summary.ts
    src/lib/agent-harness/context/questions.ts
    src/lib/agent-harness/context/decisions.ts
    src/lib/agent-harness/context/article-summaries.ts
    src/lib/agent-harness/context/blocking-relationships.ts
    src/lib/agent-harness/context/index.ts
    tests/lib/agent-harness/context.test.ts
  </files>
  <behavior>
    - Test: getProjectSummary returns project name, client, engagement type, phase, team count, date range
    - Test: getOpenQuestions returns questions filtered by status with truncation at token limit
    - Test: getArticleSummaries returns article titles + summary excerpts (pass 1 of D-19)
    - Test: getRelevantArticles takes article IDs and returns full content for top N articles (pass 2 of D-19)
    - Test: getBlockingRelationships returns questions blocking stories/epics with chain depth
    - Test: assembleContext combines multiple loaders within a total token budget
  </behavior>
  <action>
    1. Create `src/lib/agent-harness/context/project-summary.ts` (D-33):
       - `getProjectSummary(projectId: string)`: Query Project with team members count, engagement type, phase, dates
       - Return structured text summary (not raw DB rows)
       - Use scopedPrisma for project isolation

    2. Create `src/lib/agent-harness/context/questions.ts`:
       - `getOpenQuestions(projectId, limit = 20)`: Fetch questions where status != REVIEWED, ordered by priority desc + createdAt desc
       - `getQuestionContext(projectId, questionId)`: Fetch single question with linked entities for impact assessment
       - Include displayId, questionText, status, category, scope, answer (if exists)
       - Truncate questionText to ~200 chars for summary context

    3. Create `src/lib/agent-harness/context/decisions.ts`:
       - `getRecentDecisions(projectId, limit = 15)`: Fetch decisions ordered by recency
       - Include displayId, title, outcome, rationale (truncated)

    4. Create `src/lib/agent-harness/context/article-summaries.ts` implementing D-19 two-pass retrieval:
       - Pass 1: `getArticleSummaries(projectId)`: Fetch all non-stale articles with title + first 200 chars of content (summary). Lightweight.
       - Pass 2: `getRelevantArticles(projectId, articleIds: string[])`: Fetch full content for specific articles. Called AFTER pass 1 determines which articles are most relevant to the current task.
       - Token budget: Pass 1 is cheap (~2K tokens for 20 articles). Pass 2 loads full content only for top 3-5 articles.

    5. Create `src/lib/agent-harness/context/blocking-relationships.ts`:
       - `getBlockingRelationships(projectId)`: Query QuestionBlocksStory/Epic/Feature joins to find questions that block other entities
       - Return structured list with question displayId, blocked entity type + displayId, and chain depth

    6. Create `src/lib/agent-harness/context/index.ts`:
       - `assembleContext(loaders: ContextLoader[], projectId, tokenBudget)`: Run loaders in parallel, concatenate results, truncate to token budget
       - Export all individual loaders for direct use
       - Simple token estimation: 1 token ~= 4 characters (conservative)

    7. Write tests mocking Prisma responses, verifying data shaping and truncation
  </action>
  <verify>
    <automated>npx vitest run tests/lib/agent-harness/context.test.ts</automated>
  </verify>
  <done>All 5 context loaders return properly shaped data, two-pass article retrieval works (summaries first, then full content), assembleContext respects token budgets</done>
</task>

<task type="auto">
  <name>Task 2: Task definitions (Layer 1) for transcript processing, question answering, and dashboard synthesis</name>
  <files>
    src/lib/agent-harness/tasks/transcript-processing.ts
    src/lib/agent-harness/tasks/question-answering.ts
    src/lib/agent-harness/tasks/dashboard-synthesis.ts
    src/lib/agent-harness/tasks/index.ts
  </files>
  <action>
    1. Create `src/lib/agent-harness/tasks/transcript-processing.ts` per tech spec Section 3.4:
       - Export `transcriptProcessingTask: TaskDefinition`
       - taskType: "TRANSCRIPT_PROCESSING"
       - executionMode: "AGENT_LOOP" (multi-step extraction)
       - maxIterations: 10 (transcript can generate many tool calls)
       - maxRetries: 3
       - ambiguityMode: "TASK_SESSION" (user confirms in chat)
       - systemPromptTemplate: Instructions for extracting questions, decisions, requirements, risks from transcript text. Include formatting instructions for grouped output.
       - contextLoader: Loads project summary + open questions (for dedup) + recent decisions (for dedup)
       - tools: define Claude tool schemas for create_question, create_decision, create_requirement, create_risk (input_schema with Zod-like field definitions per Anthropic tool format)
       - outputValidator: Verify at least one tool call was made (empty extraction = likely failure)

    2. Create `src/lib/agent-harness/tasks/question-answering.ts` per tech spec:
       - Export `questionAnsweringTask: TaskDefinition`
       - taskType: "QUESTION_ANSWERING"
       - executionMode: "SINGLE_TURN" (one AI call for impact assessment)
       - maxIterations: 1
       - ambiguityMode: "TASK_SESSION"
       - contextLoader: Loads project summary + the specific question + related entities + article summaries (two-pass: summaries then relevant full content)
       - tools: flag_conflict, update_question_status
       - outputValidator: Verify output contains impact assessment structure

    3. Create `src/lib/agent-harness/tasks/dashboard-synthesis.ts` per tech spec Section 5:
       - Export `dashboardSynthesisTask: TaskDefinition`
       - taskType: "DASHBOARD_SYNTHESIS"
       - executionMode: "SINGLE_TURN"
       - maxIterations: 1
       - ambiguityMode: "BACKGROUND" (no user present)
       - contextLoader: Loads project summary + question stats + blocking relationships + article summaries
       - tools: none (pure text generation)
       - outputValidator: Verify output contains currentFocus and recommendedFocus sections

    4. Create `src/lib/agent-harness/tasks/index.ts` barrel export
  </action>
  <verify>
    <automated>npx tsc --noEmit src/lib/agent-harness/tasks/index.ts</automated>
  </verify>
  <done>Three task definitions export correctly, each with appropriate contextLoader, tools, execution mode, and validation. All conform to TaskDefinition interface from Plan 01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB data -> AI prompt | Project data from Prisma enters Claude system prompts |
| AI tool schemas -> API | Tool definitions sent to Claude API define what the AI can do |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | context loaders | mitigate | Context loaders use scopedPrisma — never load cross-project data. Token budgets prevent unbounded data inclusion. |
| T-02-07 | Tampering | task definitions | mitigate | Tool schemas are statically defined in code, not user-controllable. outputValidator checks AI output structure. |
| T-02-08 | Denial of Service | assembleContext | mitigate | Token budget cap prevents loading excessive data. Simple char-based estimation is conservative. |
</threat_model>

<verification>
1. `npx vitest run tests/lib/agent-harness/` — all tests pass
2. `npx tsc --noEmit` — no TypeScript errors
3. Context loaders use scopedPrisma (grep verification)
4. Two-pass article retrieval pattern confirmed in article-summaries.ts
</verification>

<success_criteria>
- Context loaders fetch scoped project data and shape it for AI consumption
- Two-pass article retrieval loads summaries first, then full content for relevant articles only
- Three task definitions conform to TaskDefinition interface with correct tools and context
- assembleContext respects token budgets by truncating
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-and-knowledge-brain/02-02-SUMMARY.md`
</output>
