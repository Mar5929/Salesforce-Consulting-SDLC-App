import { describe, it, expect } from "vitest"

describe("scopedPrisma", () => {
  it.todo("appends projectId to where clause on project-scoped models")
  it.todo("MODELS_WITH_PROJECT_ID includes all expected models")
})
