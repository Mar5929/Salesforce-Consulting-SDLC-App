import { describe, it, expect } from "vitest"

describe("encryption", () => {
  it.todo("encrypt and decrypt round-trip returns original plaintext")
  it.todo("decrypt fails with wrong projectId")
  it.todo("encrypted output format is iv:tag:ciphertext base64")
})
