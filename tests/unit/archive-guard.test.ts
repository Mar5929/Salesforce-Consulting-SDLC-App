import { describe, it, expect } from "vitest"

describe("archive-guard", () => {
  it.todo("throws error when project status is ARCHIVED")
  it.todo("does not throw when project status is ACTIVE")
  it.todo("throws with specific error message mentioning reactivation")
})
