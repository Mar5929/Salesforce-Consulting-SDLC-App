import { describe, it, expect } from "vitest"

describe("pm-dashboard-synthesis", () => {
  describe("PmDashboardData aggregation", () => {
    it.todo("aggregates stories by status")
    it.todo("aggregates stories by assignee")
    it.todo("aggregates questions by status")
    it.todo("computes knowledge coverage percentage")
    it.todo("computes sprint velocity from completed story points")
    it.todo("aggregates AI cost over time from SessionLog")
    it.todo("aggregates AI cost by feature area from sessionType")
    it.todo("aggregates QA pass/fail/blocked counts")
    it.todo("aggregates defects by severity")
    it.todo("stores result in Project.cachedBriefingContent.pmDashboard")
  })
})
